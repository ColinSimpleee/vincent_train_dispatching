# 阶段一：前端治本 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `PhysicsEngine` 抽成 `packages/engine/` 零依赖纯函数模块，引入 PRNG + seed 让游戏 100% 可复现，加 8 条不变量断言 + 结构化事件流替代 `console.log`，提供 Replay 面板定位偶发 bug，把 `App.vue` 拆为 6 个职责单一模块，最后用 100 局模糊测试守住引擎的稳定性。

**Architecture:** 物理引擎成为零依赖纯模块（不引用 Vue / 浏览器 API / `Math.random` / `Date.now`），由 `step(state, inputs) → { next, events, violations }` 单一入口暴露。前端 UI 层用 Vue `reactive` 包一层 `EngineState` 副本驱动渲染，引擎不感知 Vue。`Math.random` 全面替换为基于 `mulberry32` 的 PRNG，状态 `rng` 跟随 `EngineState`。`App.vue` 减肥到 < 200 行，业务逻辑分入 `src/game/` 6 模块。

**Tech Stack:** TypeScript 5.9、Vue 3.5、Vitest 4 + JSDOM、Vue Test Utils、Vite 7。无新增依赖。

---

## 仓库结构变更

阶段一**不**做 monorepo 拆分（那是阶段二），但**预先建立 `packages/engine/` 目录** 并通过 `tsconfig` paths + `vite alias` 引用。这样阶段二搬 `src/` 到 `frontend/` 时 `packages/engine/` 已就位、零迁移。

```
列车调度游戏v2.0/
├── packages/
│   └── engine/                       # 阶段一新增（仓库根级）
│       ├── package.json              # 标识为本地包
│       ├── tsconfig.json
│       ├── src/
│       │   ├── index.ts              # 公共 API 出口
│       │   ├── types.ts              # EngineState/Input/Event/Invariant
│       │   ├── prng.ts               # mulberry32 + 包装方法
│       │   ├── adjacency.ts          # 邻接表纯函数
│       │   ├── events.ts             # 事件环形缓冲
│       │   ├── invariants.ts         # 8 条不变量
│       │   ├── step.ts               # 主入口 step()
│       │   └── phases/
│       │       ├── passenger.ts
│       │       ├── intent.ts
│       │       ├── collision.ts
│       │       ├── conflict.ts
│       │       └── commit.ts
│       └── tests/                    # 引擎单测（覆盖率 ≥ 80%）
│           ├── prng.spec.ts
│           ├── adjacency.spec.ts
│           ├── events.spec.ts
│           ├── invariants.spec.ts
│           ├── phases/
│           │   ├── passenger.spec.ts
│           │   ├── intent.spec.ts
│           │   ├── collision.spec.ts
│           │   ├── conflict.spec.ts
│           │   └── commit.spec.ts
│           ├── step.spec.ts
│           ├── golden-paths.spec.ts  # 黄金路径
│           ├── regressions.spec.ts   # 历史 bug 回归
│           └── fuzz.spec.ts          # 模糊测试
├── src/
│   ├── game/                         # 阶段一新增
│   │   ├── EventBus.ts
│   │   ├── InputRecorder.ts
│   │   ├── GameLoop.ts
│   │   ├── TrainSpawner.ts
│   │   ├── DispatchController.ts
│   │   └── ReplayPlayer.ts
│   ├── components/
│   │   └── ReplayPanel.vue           # 阶段一新增（dev-only）
│   ├── core/                         # 现存目录，逐步瘦身
│   │   ├── PhysicsEngine.ts          # 任务 14 之后变成空壳/删除
│   │   ├── ScheduleManager.ts
│   │   ├── RailGraph.ts
│   │   ├── types.ts
│   │   ├── constants.ts
│   │   └── utils.ts
│   └── App.vue                       # 缩减到 < 200 行
└── ...
```

**模块责任**

| 模块 | 责任 | 不该做的事 |
|---|---|---|
| `packages/engine/src/prng.ts` | 确定性伪随机数算法及包装 | 调用 `Math.random()` / `Date.now()` |
| `packages/engine/src/types.ts` | 引擎对外类型契约 | 引用 Vue / 浏览器 |
| `packages/engine/src/adjacency.ts` | 节点 → 入边/出边索引 | 持有可变全局状态（必须接收 map 返回新结构） |
| `packages/engine/src/events.ts` | 环形事件缓冲 + 事件类型 | 与 UI 耦合 |
| `packages/engine/src/invariants.ts` | 8 条状态机不变量 | 抛错（仅返回 violations） |
| `packages/engine/src/phases/*.ts` | 5 个 phase 的纯函数 | 共享可变状态、随机性外溢 |
| `packages/engine/src/step.ts` | 组装 5 个 phase 的入口 | 引用 Vue / DOM |
| `src/game/EventBus.ts` | 引擎事件 → UI 订阅桥 | 写引擎逻辑 |
| `src/game/InputRecorder.ts` | 采集 EngineInput 流并打包 | 转换业务概念（业务逻辑在 DispatchController） |
| `src/game/GameLoop.ts` | `requestAnimationFrame` + 累加器 + 倍速 | 调度生成逻辑 |
| `src/game/TrainSpawner.ts` | 把 ScheduleEntry 转 EngineInput.admit | 物理逻辑 |
| `src/game/DispatchController.ts` | ADMIT/DEPART/STOP 按钮业务 | UI 渲染 |
| `src/game/ReplayPlayer.ts` | 给定 seed+inputs 重放并对比事件流 | 与运行时 GameLoop 共享状态 |
| `src/components/ReplayPanel.vue` | dev-only 面板 UI | 业务逻辑 |

---

## Phase 总览

| Phase | 任务范围 | 任务编号 |
|---|---|---|
| A | 基础设施：建立 packages/engine 工程 | 1 |
| B | PRNG 模块 | 2–3 |
| C | 引擎类型 + 工具模块 | 4–6 |
| D | 引擎纯函数化（5 个 phase + step） | 7–12 |
| E | 切换运行时到新引擎、PRNG 替换 Math.random | 13–14 |
| F | 不变量断言（框架 + 7 条规则） | 15–22 |
| G | 事件流接入 + 导出按钮 | 23 |
| H | App.vue 拆解为 6 模块 | 24–29 |
| I | Replay 工具（Player + 面板） | 30–31 |
| J | 模糊测试 100 局 | 32 |
| K | 验收：历史 bug 复现 + 修复 | 33 |

每个任务后必有 commit。所有 commit message 中文，不含署名。

---

## Phase A：基础设施

### Task 1：建立 packages/engine 目录与构建配置

**Files:**
- Create: `packages/engine/package.json`
- Create: `packages/engine/tsconfig.json`
- Create: `packages/engine/src/index.ts`
- Create: `packages/engine/tests/sanity.spec.ts`
- Modify: `tsconfig.app.json`
- Modify: `tsconfig.vitest.json`
- Modify: `vite.config.ts`

- [ ] **Step 1：创建 `packages/engine/package.json`**

```json
{
  "name": "@headway/engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  }
}
```

- [ ] **Step 2：创建 `packages/engine/tsconfig.json`**

```json
{
  "extends": "../../tsconfig.app.json",
  "include": ["src/**/*"],
  "exclude": ["tests/**/*"],
  "compilerOptions": {
    "composite": false,
    "noEmit": true,
    "lib": ["ES2022"]
  }
}
```

- [ ] **Step 3：创建 `packages/engine/src/index.ts` 占位**

```ts
// Public API surface — empty until later tasks fill it in
export const ENGINE_VERSION = '0.1.0'
```

- [ ] **Step 4：在 `tsconfig.app.json` 的 `paths` 加 alias**

修改 `compilerOptions.paths`：

```json
"paths": {
  "@/*": ["./src/*"],
  "@engine": ["./packages/engine/src/index.ts"],
  "@engine/*": ["./packages/engine/src/*"]
},
"include": ["env.d.ts", "src/**/*", "src/**/*.vue", "packages/engine/src/**/*"]
```

- [ ] **Step 5：在 `tsconfig.vitest.json` 镜像同样的 paths 与 include**

打开 `tsconfig.vitest.json`，把 `paths` 与 `include` 用同样配置覆盖（继承自 app 通常就够，确认即可，不够则补）。

- [ ] **Step 6：在 `vite.config.ts` 加 alias**

```ts
import { fileURLToPath, URL } from 'node:url'
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import vueDevTools from 'vite-plugin-vue-devtools'

export default defineConfig({
  plugins: [vue(), vueDevTools()],
  resolve: {
    alias: {
      '@': fileURLToPath(new URL('./src', import.meta.url)),
      '@engine': fileURLToPath(new URL('./packages/engine/src/index.ts', import.meta.url)),
      '@engine/': fileURLToPath(new URL('./packages/engine/src/', import.meta.url)),
    },
  },
  server: { host: '127.0.0.1', port: 5173 },
})
```

- [ ] **Step 7：写 sanity 测试 `packages/engine/tests/sanity.spec.ts`**

```ts
import { describe, expect, it } from 'vitest'
import { ENGINE_VERSION } from '@engine'

describe('engine package wiring', () => {
  it('exports ENGINE_VERSION', () => {
    expect(ENGINE_VERSION).toBe('0.1.0')
  })
})
```

- [ ] **Step 8：跑测试确认通过**

Run: `npx vitest run packages/engine/tests/sanity.spec.ts`
Expected: 1 passed

- [ ] **Step 9：跑 `npm run build` 确认 alias 不破坏构建**

Run: `npm run build`
Expected: 构建成功

- [ ] **Step 10：commit**

```bash
git add packages/engine tsconfig.app.json tsconfig.vitest.json vite.config.ts
git commit -m "feat(engine): 建立 packages/engine 工程骨架与 alias"
```

---

## Phase B：PRNG 模块

### Task 2：mulberry32 算法（确定性伪随机数）

**Files:**
- Create: `packages/engine/src/prng.ts`
- Create: `packages/engine/tests/prng.spec.ts`

- [ ] **Step 1：写测试 `packages/engine/tests/prng.spec.ts`（仅核心算法部分）**

```ts
import { describe, expect, it } from 'vitest'
import { createPRNG, nextUint32, clonePRNG } from '@engine/prng'

describe('mulberry32 PRNG', () => {
  it('相同 seed 产生相同序列', () => {
    const a = createPRNG(42)
    const b = createPRNG(42)
    const seqA: number[] = []
    const seqB: number[] = []
    for (let i = 0; i < 100; i++) {
      seqA.push(nextUint32(a))
      seqB.push(nextUint32(b))
    }
    expect(seqA).toEqual(seqB)
  })

  it('不同 seed 产生不同序列', () => {
    const a = createPRNG(42)
    const b = createPRNG(43)
    const va: number[] = []
    const vb: number[] = []
    for (let i = 0; i < 32; i++) {
      va.push(nextUint32(a))
      vb.push(nextUint32(b))
    }
    expect(va).not.toEqual(vb)
  })

  it('clonePRNG 后两份独立推进互不干扰', () => {
    const a = createPRNG(7)
    nextUint32(a)
    const b = clonePRNG(a)
    nextUint32(a)
    nextUint32(a)
    const before = b.state
    nextUint32(b)
    expect(b.state).not.toBe(before)
    // a 多推进 2 步不会反向影响 b
    expect(clonePRNG(a).state).not.toBe(b.state)
  })

  it('mulberry32 第一个输出对 seed=1 是已知常数（防回归）', () => {
    const rng = createPRNG(1)
    const v = nextUint32(rng)
    // mulberry32(1) 第一步：state = 1 + 0x6D2B79F5 = 0x6D2B79F6
    // 详见实现注释
    expect(v).toBe(1791095845)
  })
})
```

- [ ] **Step 2：跑测试确认失败（模块尚未存在）**

Run: `npx vitest run packages/engine/tests/prng.spec.ts`
Expected: 4 failed（"Cannot find module '@engine/prng'"）

- [ ] **Step 3：实现 `packages/engine/src/prng.ts`**

```ts
/**
 * mulberry32：32 位整数状态的快速 PRNG。
 * 选它的理由：实现 12 行、无外部依赖、给定 seed 跨平台逐位一致；
 * 不适用于密码学场景，但游戏物理够用。
 */

export interface PRNGState {
  /** 32 位无符号整数当前状态。`>>> 0` 强制无符号化。 */
  state: number
}

export function createPRNG(seed: number): PRNGState {
  return { state: seed >>> 0 }
}

export function clonePRNG(rng: PRNGState): PRNGState {
  return { state: rng.state >>> 0 }
}

/** 推进一次，返回 32 位无符号整数；mutates rng.state。 */
export function nextUint32(rng: PRNGState): number {
  rng.state = (rng.state + 0x6d2b79f5) >>> 0
  let t = rng.state
  t = Math.imul(t ^ (t >>> 15), t | 1)
  t ^= t + Math.imul(t ^ (t >>> 7), t | 61)
  return ((t ^ (t >>> 14)) >>> 0)
}
```

- [ ] **Step 4：跑测试确认通过**

Run: `npx vitest run packages/engine/tests/prng.spec.ts`
Expected: 4 passed

- [ ] **Step 5：commit**

```bash
git add packages/engine/src/prng.ts packages/engine/tests/prng.spec.ts
git commit -m "feat(engine): 添加 mulberry32 PRNG 算法"
```

---

### Task 3：PRNG 包装方法（randomFloat / randomInt / randomChoice）

**Files:**
- Modify: `packages/engine/src/prng.ts`
- Modify: `packages/engine/tests/prng.spec.ts`

- [ ] **Step 1：在测试文件追加用例**

```ts
import { randomFloat, randomInt, randomChoice, randomChance } from '@engine/prng'

describe('PRNG 包装方法', () => {
  it('randomFloat ∈ [0, 1)', () => {
    const rng = createPRNG(123)
    for (let i = 0; i < 1000; i++) {
      const v = randomFloat(rng)
      expect(v).toBeGreaterThanOrEqual(0)
      expect(v).toBeLessThan(1)
    }
  })

  it('randomInt(min, maxInclusive) 的边界', () => {
    const rng = createPRNG(123)
    const counts = new Map<number, number>()
    for (let i = 0; i < 10000; i++) {
      const v = randomInt(rng, 5, 10)
      expect(v).toBeGreaterThanOrEqual(5)
      expect(v).toBeLessThanOrEqual(10)
      counts.set(v, (counts.get(v) ?? 0) + 1)
    }
    // 6 个值都应至少出现过
    expect(counts.size).toBe(6)
  })

  it('randomChoice 永远从输入数组取', () => {
    const rng = createPRNG(123)
    const arr = ['A', 'B', 'C']
    const seen = new Set<string>()
    for (let i = 0; i < 100; i++) {
      seen.add(randomChoice(rng, arr))
    }
    for (const s of seen) expect(arr).toContain(s)
  })

  it('randomChance(p) 概率与 p 大致一致', () => {
    const rng = createPRNG(7)
    let trues = 0
    for (let i = 0; i < 10000; i++) {
      if (randomChance(rng, 0.3)) trues++
    }
    // 0.3 ± 0.02 → [0.28, 0.32]
    expect(trues / 10000).toBeGreaterThan(0.27)
    expect(trues / 10000).toBeLessThan(0.33)
  })

  it('包装方法与 nextUint32 一致地推进同一 state', () => {
    const a = createPRNG(99)
    randomFloat(a)
    randomInt(a, 0, 100)
    const after = a.state

    const b = createPRNG(99)
    nextUint32(b)
    nextUint32(b)
    expect(b.state).toBe(after)
  })
})
```

- [ ] **Step 2：跑测试确认失败**

Run: `npx vitest run packages/engine/tests/prng.spec.ts`
Expected: 5 failed

- [ ] **Step 3：在 `packages/engine/src/prng.ts` 追加实现**

```ts
/** 浮点 ∈ [0, 1)。等价于 nextUint32(rng) / 2^32。 */
export function randomFloat(rng: PRNGState): number {
  return nextUint32(rng) / 0x100000000
}

/** 整数 ∈ [min, maxInclusive]。 */
export function randomInt(rng: PRNGState, min: number, maxInclusive: number): number {
  if (maxInclusive < min) throw new Error('randomInt: max < min')
  const range = maxInclusive - min + 1
  return min + (nextUint32(rng) % range)
}

/** 从非空数组等概率取一个。 */
export function randomChoice<T>(rng: PRNGState, arr: readonly T[]): T {
  if (arr.length === 0) throw new Error('randomChoice: empty array')
  return arr[randomInt(rng, 0, arr.length - 1)]!
}

/** Bernoulli：以 p 概率返回 true。p ∈ [0, 1]。 */
export function randomChance(rng: PRNGState, p: number): boolean {
  return randomFloat(rng) < p
}
```

- [ ] **Step 4：跑测试确认通过**

Run: `npx vitest run packages/engine/tests/prng.spec.ts`
Expected: 9 passed

- [ ] **Step 5：在 `packages/engine/src/index.ts` 重新导出**

```ts
export const ENGINE_VERSION = '0.1.0'
export type { PRNGState } from './prng'
export {
  createPRNG,
  clonePRNG,
  nextUint32,
  randomFloat,
  randomInt,
  randomChoice,
  randomChance,
} from './prng'
```

- [ ] **Step 6：commit**

```bash
git add packages/engine/src/prng.ts packages/engine/src/index.ts packages/engine/tests/prng.spec.ts
git commit -m "feat(engine): 添加 PRNG 包装方法 randomFloat/randomInt/randomChoice/randomChance"
```

---

## Phase C：引擎类型 + 工具模块

### Task 4：引擎类型契约

**Files:**
- Create: `packages/engine/src/types.ts`
- Modify: `packages/engine/src/index.ts`

- [ ] **Step 1：创建 `packages/engine/src/types.ts`**

```ts
import type { PRNGState } from './prng'

// ============================================================================
// 1. 物理与拓扑（从 src/core/RailGraph.ts 复刻；阶段一并存，阶段二删除原版）
// ============================================================================

export interface RailNode {
  id: string
  x: number
  y: number
  type: 'switch' | 'endpoint' | 'connector' | 'buffer_stop'
  switchState?: number
  signalState?: 'red' | 'green'
  groupId?: string
}

export interface SwitchGroupMember {
  nodeId: string
  states: number[]
}
export interface SwitchGroup {
  id: string
  masterNodeId: string
  members: SwitchGroupMember[]
}

export interface RailEdge {
  id: string
  fromNode: string
  toNode: string
  length: number
  occupiedBy: string | null
  isPlatform?: boolean
  control1?: { x: number; y: number }
  control2?: { x: number; y: number }
}

export interface PlatformZone {
  id: string
  label: string
  rect: { x: number; y: number; w: number; h: number }
}

export interface RailMap {
  nodes: Record<string, RailNode>
  edges: Record<string, RailEdge>
  platforms: PlatformZone[]
  switchGroups?: SwitchGroup[]
}

export type TrainModel = 'CR400AF' | 'CR400BF' | 'CRH380A'

export interface TrainPhysics {
  id: string
  modelType: TrainModel
  isCoupled: boolean
  currentEdgeId: string
  position: number
  speed: number
  state: 'moving' | 'stopped'
  direction: 1 | -1
  path: string[]
  visitedPath: string[]
  nextMoveIntent?: { targetEdgeId: string; overflowDistance: number }
  passengerState?: 'BOARDING' | 'READY'
  boardingTimer?: number
  lastServicedEdgeId?: string
  isHandedOver?: boolean
  scheduleEntryId?: string
  scheduledArriveTick?: number
  arrivalTick?: number
  stopDuration?: number
  stopBuffer?: number
  /**
   * 不变量观察用：连续静止的 tick 数；commit phase 维护。
   * 不参与序列化对外，但跨 tick 持续。
   */
  staleStoppedTicks?: number
  /**
   * 不变量观察用：移交后已存在的 tick 数。
   */
  postHandoverTicks?: number
}

// ============================================================================
// 2. 引擎对外契约
// ============================================================================

export interface EngineState {
  /** 已完成的 tick 计数（即将进入第 tick+1 个 tick 时 step 接收的 state.tick == 当前已完成数） */
  tick: number
  trains: TrainPhysics[]
  railMap: RailMap
  rng: PRNGState
}

export type EngineInput =
  | { tick: number; type: 'admit'; payload: { trainSpec: TrainSpawnSpec } }
  | { tick: number; type: 'depart'; payload: { trainId: string; targetExitNodeId: string } }
  | { tick: number; type: 'stop'; payload: { trainId: string } }
  | { tick: number; type: 'switch_toggle'; payload: { nodeId: string } }
  | { tick: number; type: 'signal_toggle'; payload: { nodeId: string } }

/** 由 TrainSpawner 构造、admit 输入携带的列车规格。 */
export interface TrainSpawnSpec {
  id: string
  modelType: TrainModel
  isCoupled: boolean
  currentEdgeId: string
  path: string[]
  direction: 1 | -1
  speed: number
  scheduleEntryId?: string
  scheduledArriveTick?: number
}

export type EngineEvent =
  | { tick: number; kind: 'spawn'; trainId: string; modelType: TrainModel; entrance: string }
  | { tick: number; kind: 'admit'; trainId: string; edgeId: string }
  | { tick: number; kind: 'enter_edge'; trainId: string; from: string; to: string }
  | { tick: number; kind: 'signal_pass'; trainId: string; nodeId: string; signalState: 'red' | 'green' }
  | { tick: number; kind: 'dwell_start'; trainId: string; edgeId: string; duration: number }
  | { tick: number; kind: 'dwell_end'; trainId: string; edgeId: string }
  | { tick: number; kind: 'depart'; trainId: string; strategy: 'direct' | 'reverse' | 'standard' | 'fallback' }
  | { tick: number; kind: 'handover'; trainId: string; edgeId: string }
  | { tick: number; kind: 'remove'; trainId: string; edgeId: string }
  | { tick: number; kind: 'stop_command'; trainId: string }
  | { tick: number; kind: 'switch_toggled'; nodeId: string; newState: number }
  | { tick: number; kind: 'signal_toggled'; nodeId: string; newState: 'red' | 'green' }
  | { tick: number; kind: 'collision'; trainA: string; trainB: string; edgeId: string }
  | { tick: number; kind: 'invariant_violation'; rule: string; detail: Record<string, unknown> }

export interface Invariant {
  rule: string
  detail: Record<string, unknown>
}

export interface StepResult {
  next: EngineState
  events: EngineEvent[]
  violations: Invariant[]
}
```

- [ ] **Step 2：在 `packages/engine/src/index.ts` 重新导出**

```ts
export const ENGINE_VERSION = '0.1.0'
export type { PRNGState } from './prng'
export {
  createPRNG,
  clonePRNG,
  nextUint32,
  randomFloat,
  randomInt,
  randomChoice,
  randomChance,
} from './prng'
export type {
  RailNode,
  RailEdge,
  RailMap,
  PlatformZone,
  SwitchGroup,
  SwitchGroupMember,
  TrainModel,
  TrainPhysics,
  EngineState,
  EngineInput,
  EngineEvent,
  Invariant,
  StepResult,
  TrainSpawnSpec,
} from './types'
```

- [ ] **Step 3：跑全量类型检查确认通过**

Run: `npm run type-check`
Expected: 无类型错误

- [ ] **Step 4：commit**

```bash
git add packages/engine/src/types.ts packages/engine/src/index.ts
git commit -m "feat(engine): 定义 EngineState/Input/Event/Invariant 类型契约"
```

---

### Task 5：邻接表纯函数化

**Files:**
- Create: `packages/engine/src/adjacency.ts`
- Create: `packages/engine/tests/adjacency.spec.ts`

- [ ] **Step 1：写测试 `packages/engine/tests/adjacency.spec.ts`**

```ts
import { describe, expect, it } from 'vitest'
import { buildAdjacency, resolveNextEdge } from '@engine/adjacency'
import type { RailMap, RailNode } from '@engine'

function makeMap(): RailMap {
  const node = (id: string, type: RailNode['type'] = 'connector', extra: Partial<RailNode> = {}): RailNode => ({
    id, x: 0, y: 0, type, ...extra,
  })
  return {
    nodes: {
      a: node('a'),
      b: node('b', 'switch', { switchState: 0 }),
      c: node('c'),
      d: node('d'),
    },
    edges: {
      e_ab: { id: 'e_ab', fromNode: 'a', toNode: 'b', length: 100, occupiedBy: null },
      e_bc: { id: 'e_bc', fromNode: 'b', toNode: 'c', length: 100, occupiedBy: null },
      e_bd: { id: 'e_bd', fromNode: 'b', toNode: 'd', length: 100, occupiedBy: null },
    },
    platforms: [],
  }
}

describe('buildAdjacency', () => {
  it('每个节点列出 outgoing/incoming 边', () => {
    const adj = buildAdjacency(makeMap())
    expect(adj.get('b')?.outgoing.map((e) => e.id).sort()).toEqual(['e_bc', 'e_bd'])
    expect(adj.get('b')?.incoming.map((e) => e.id)).toEqual(['e_ab'])
  })

  it('outgoing/incoming 按 id 排序（决定性）', () => {
    const adj = buildAdjacency(makeMap())
    expect(adj.get('b')?.outgoing.map((e) => e.id)).toEqual(['e_bc', 'e_bd'])
  })
})

describe('resolveNextEdge', () => {
  it('正向到达分岔点 + switchState=0 → 选第一条出边', () => {
    const map = makeMap()
    const adj = buildAdjacency(map)
    const next = resolveNextEdge(map.nodes.b!, 'e_ab', adj)
    expect(next).toBe('e_bc')
  })

  it('switchState=1 → 选第二条出边', () => {
    const map = makeMap()
    map.nodes.b!.switchState = 1
    const adj = buildAdjacency(map)
    const next = resolveNextEdge(map.nodes.b!, 'e_ab', adj)
    expect(next).toBe('e_bd')
  })

  it('反向（trailing）到达：忽略 switchState，取第一个候选', () => {
    const map = makeMap()
    const adj = buildAdjacency(map)
    // 列车在 e_bc 上反向行驶，到达 b 节点 → b 的 incoming 中除 e_bc 之外
    const next = resolveNextEdge(map.nodes.b!, 'e_bc', adj)
    // 唯一的 incoming 候选是 e_ab
    expect(next).toBe('e_ab')
  })

  it('单一候选无视 switchState', () => {
    const map = makeMap()
    map.nodes.b!.switchState = 99
    const adj = buildAdjacency(map)
    // 从 c 到 b 反向 → b.incoming 除 e_bc 外只有 e_ab
    const next = resolveNextEdge(map.nodes.b!, 'e_bc', adj)
    expect(next).toBe('e_ab')
  })

  it('无候选返回 undefined', () => {
    const map: RailMap = {
      nodes: { x: { id: 'x', x: 0, y: 0, type: 'endpoint' } },
      edges: {
        e_xy: { id: 'e_xy', fromNode: 'x', toNode: 'x', length: 1, occupiedBy: null },
      },
      platforms: [],
    }
    const adj = buildAdjacency(map)
    const next = resolveNextEdge(map.nodes.x!, 'e_xy', adj)
    expect(next).toBeUndefined()
  })
})
```

- [ ] **Step 2：跑测试确认失败**

Run: `npx vitest run packages/engine/tests/adjacency.spec.ts`
Expected: 7 failed

- [ ] **Step 3：实现 `packages/engine/src/adjacency.ts`**

```ts
import type { RailMap, RailNode } from './types'

export interface AdjacencyEntry {
  outgoing: { id: string; fromNode: string; toNode: string }[]
  incoming: { id: string; fromNode: string; toNode: string }[]
}

export type AdjacencyIndex = Map<string, AdjacencyEntry>

/**
 * 构造节点 → 入边/出边索引。结果按边 id 字典序排序，保证可重现。
 * 不缓存、不持有 RailMap 引用，调用方自行决定何时重建。
 */
export function buildAdjacency(map: RailMap): AdjacencyIndex {
  const adj: AdjacencyIndex = new Map()
  for (const edge of Object.values(map.edges)) {
    const ref = { id: edge.id, fromNode: edge.fromNode, toNode: edge.toNode }
    let from = adj.get(edge.fromNode)
    if (!from) {
      from = { outgoing: [], incoming: [] }
      adj.set(edge.fromNode, from)
    }
    from.outgoing.push(ref)
    let to = adj.get(edge.toNode)
    if (!to) {
      to = { outgoing: [], incoming: [] }
      adj.set(edge.toNode, to)
    }
    to.incoming.push(ref)
  }
  for (const entry of adj.values()) {
    entry.outgoing.sort((a, b) => a.id.localeCompare(b.id))
    entry.incoming.sort((a, b) => a.id.localeCompare(b.id))
  }
  return adj
}

/**
 * 解析下一条边。
 * - 列车从 incomingEdge 到达 node：
 *   - 若 incomingEdge 在 node.outgoing 中（即 incomingEdge.fromNode === node.id）
 *     → 列车实际反向行驶，下一条边在 node.incoming 中
 *   - 否则正向到达，下一条边在 node.outgoing 中
 * - 多个候选 + node.type === 'switch' → 用 node.switchState 选第 i 个（取模）
 * - 多个候选 + 非 switch（trailing） → 第一个
 */
export function resolveNextEdge(
  node: RailNode | undefined,
  incomingEdgeId: string,
  adj: AdjacencyIndex,
): string | undefined {
  if (!node) return undefined
  const entry = adj.get(node.id)
  if (!entry) return undefined

  const arrivedAtFromNode = entry.outgoing.some((e) => e.id === incomingEdgeId)
  const candidates = arrivedAtFromNode
    ? entry.incoming.filter((e) => e.id !== incomingEdgeId)
    : entry.outgoing.filter((e) => e.id !== incomingEdgeId)

  if (candidates.length === 0) return undefined
  if (candidates.length === 1) return candidates[0]!.id

  if (node.type === 'switch') {
    const i = (node.switchState ?? 0) % candidates.length
    return candidates[i]!.id
  }
  return candidates[0]!.id
}
```

- [ ] **Step 4：在 `packages/engine/src/index.ts` 导出**

追加：

```ts
export type { AdjacencyEntry, AdjacencyIndex } from './adjacency'
export { buildAdjacency, resolveNextEdge } from './adjacency'
```

- [ ] **Step 5：跑测试确认通过**

Run: `npx vitest run packages/engine/tests/adjacency.spec.ts`
Expected: 7 passed

- [ ] **Step 6：commit**

```bash
git add packages/engine/src/adjacency.ts packages/engine/src/index.ts packages/engine/tests/adjacency.spec.ts
git commit -m "feat(engine): 抽取邻接表为纯函数 buildAdjacency/resolveNextEdge"
```

---

### Task 6：事件环形缓冲

**Files:**
- Create: `packages/engine/src/events.ts`
- Create: `packages/engine/tests/events.spec.ts`

- [ ] **Step 1：写测试 `packages/engine/tests/events.spec.ts`**

```ts
import { describe, expect, it } from 'vitest'
import type { EngineEvent } from '@engine'
import { EventBuffer } from '@engine/events'

function ev(tick: number, kind: 'spawn' = 'spawn'): EngineEvent {
  return { tick, kind, trainId: 't', modelType: 'CR400AF', entrance: 'L' }
}

describe('EventBuffer', () => {
  it('push 后 snapshot 顺序与压入顺序一致', () => {
    const b = new EventBuffer(10)
    b.push(ev(1))
    b.push(ev(2))
    b.push(ev(3))
    expect(b.snapshot().map((e) => e.tick)).toEqual([1, 2, 3])
  })

  it('超过容量时丢弃最早的事件', () => {
    const b = new EventBuffer(3)
    for (let i = 1; i <= 5; i++) b.push(ev(i))
    expect(b.snapshot().map((e) => e.tick)).toEqual([3, 4, 5])
  })

  it('size 跟踪当前长度', () => {
    const b = new EventBuffer(3)
    expect(b.size).toBe(0)
    b.push(ev(1))
    b.push(ev(2))
    expect(b.size).toBe(2)
    b.push(ev(3))
    b.push(ev(4))
    expect(b.size).toBe(3)
  })

  it('clear 清空但保留容量', () => {
    const b = new EventBuffer(3)
    b.push(ev(1))
    b.clear()
    expect(b.size).toBe(0)
    b.push(ev(2))
    expect(b.snapshot().map((e) => e.tick)).toEqual([2])
  })

  it('snapshot 返回独立副本（外部修改不影响内部）', () => {
    const b = new EventBuffer(3)
    b.push(ev(1))
    const s = b.snapshot()
    s.push(ev(99))
    expect(b.snapshot()).toHaveLength(1)
  })

  it('toJSON 序列化为有序数组', () => {
    const b = new EventBuffer(3)
    b.push(ev(1))
    b.push(ev(2))
    expect(JSON.stringify(b.toJSON())).toBe(JSON.stringify([ev(1), ev(2)]))
  })
})
```

- [ ] **Step 2：跑测试确认失败**

Run: `npx vitest run packages/engine/tests/events.spec.ts`
Expected: 6 failed

- [ ] **Step 3：实现 `packages/engine/src/events.ts`**

```ts
import type { EngineEvent } from './types'

/**
 * 环形事件缓冲。最近 N 条事件，超出容量丢弃最旧。
 * 不与 Vue / DOM 耦合；UI 侧通过 snapshot() 取副本驱动渲染。
 */
export class EventBuffer {
  private readonly capacity: number
  private buf: (EngineEvent | undefined)[]
  private head = 0      // 下一个写入位置
  private count = 0     // 当前元素数量

  constructor(capacity: number) {
    if (!Number.isInteger(capacity) || capacity <= 0) {
      throw new Error(`EventBuffer: capacity 必须为正整数，收到 ${capacity}`)
    }
    this.capacity = capacity
    this.buf = new Array(capacity)
  }

  get size(): number {
    return this.count
  }

  push(event: EngineEvent): void {
    this.buf[this.head] = event
    this.head = (this.head + 1) % this.capacity
    if (this.count < this.capacity) this.count++
  }

  /** 返回时间序快照副本。 */
  snapshot(): EngineEvent[] {
    const result: EngineEvent[] = new Array(this.count)
    const start = this.count < this.capacity ? 0 : this.head
    for (let i = 0; i < this.count; i++) {
      result[i] = this.buf[(start + i) % this.capacity]!
    }
    return result
  }

  clear(): void {
    this.buf = new Array(this.capacity)
    this.head = 0
    this.count = 0
  }

  toJSON(): EngineEvent[] {
    return this.snapshot()
  }
}
```

- [ ] **Step 4：在 `packages/engine/src/index.ts` 导出**

追加：

```ts
export { EventBuffer } from './events'
```

- [ ] **Step 5：跑测试确认通过**

Run: `npx vitest run packages/engine/tests/events.spec.ts`
Expected: 6 passed

- [ ] **Step 6：commit**

```bash
git add packages/engine/src/events.ts packages/engine/src/index.ts packages/engine/tests/events.spec.ts
git commit -m "feat(engine): 添加 EventBuffer 环形事件缓冲"
```

---

## Phase D：引擎纯函数化

### Task 7：phase passenger 纯函数

**Files:**
- Create: `packages/engine/src/phases/passenger.ts`
- Create: `packages/engine/tests/phases/passenger.spec.ts`

- [ ] **Step 1：写测试 `packages/engine/tests/phases/passenger.spec.ts`**

```ts
import { describe, expect, it } from 'vitest'
import type { EngineEvent, TrainPhysics } from '@engine'
import { handlePassenger } from '@engine/phases/passenger'

function baseTrain(over: Partial<TrainPhysics> = {}): TrainPhysics {
  return {
    id: 't1',
    modelType: 'CR400AF',
    isCoupled: false,
    currentEdgeId: 't1',
    position: 0,
    speed: 0,
    state: 'stopped',
    direction: 1,
    path: [],
    visitedPath: [],
    ...over,
  }
}

describe('handlePassenger', () => {
  it('BOARDING + boardingTimer > 0 → 递减 1，状态不变', () => {
    const t = baseTrain({ passengerState: 'BOARDING', boardingTimer: 100, currentEdgeId: 't1' })
    const events: EngineEvent[] = []
    handlePassenger(t, 50, events)
    expect(t.boardingTimer).toBe(99)
    expect(t.passengerState).toBe('BOARDING')
    expect(events).toHaveLength(0)
  })

  it('BOARDING + boardingTimer 到 0 → 转 READY 并发出 dwell_end', () => {
    const t = baseTrain({ passengerState: 'BOARDING', boardingTimer: 1, currentEdgeId: 't1' })
    const events: EngineEvent[] = []
    handlePassenger(t, 50, events)
    expect(t.boardingTimer).toBe(0)
    expect(t.passengerState).toBe('READY')
    expect(events).toEqual([{ tick: 50, kind: 'dwell_end', trainId: 't1', edgeId: 't1' }])
  })

  it('非 BOARDING 状态不动', () => {
    const t = baseTrain({ passengerState: 'READY', boardingTimer: 100 })
    const events: EngineEvent[] = []
    handlePassenger(t, 50, events)
    expect(t.boardingTimer).toBe(100)
    expect(events).toHaveLength(0)
  })

  it('boardingTimer 未定义视作 0 → 立即 READY', () => {
    const t = baseTrain({ passengerState: 'BOARDING' })
    const events: EngineEvent[] = []
    handlePassenger(t, 50, events)
    expect(t.passengerState).toBe('READY')
    expect(events).toHaveLength(1)
  })
})
```

- [ ] **Step 2：跑测试确认失败**

Run: `npx vitest run packages/engine/tests/phases/passenger.spec.ts`
Expected: 4 failed

- [ ] **Step 3：实现 `packages/engine/src/phases/passenger.ts`**

```ts
import type { EngineEvent, TrainPhysics } from '../types'

/**
 * Phase 0：乘客逻辑。
 * BOARDING 状态每 tick 递减 boardingTimer；归零转 READY 并发出 dwell_end 事件。
 * mutates train。
 */
export function handlePassenger(
  train: TrainPhysics,
  tick: number,
  events: EngineEvent[],
): void {
  if (train.passengerState !== 'BOARDING') return
  const next = (train.boardingTimer ?? 0) - 1
  train.boardingTimer = next > 0 ? next : 0
  if (train.boardingTimer === 0) {
    train.passengerState = 'READY'
    events.push({
      tick,
      kind: 'dwell_end',
      trainId: train.id,
      edgeId: train.currentEdgeId,
    })
  }
}
```

- [ ] **Step 4：跑测试确认通过**

Run: `npx vitest run packages/engine/tests/phases/passenger.spec.ts`
Expected: 4 passed

- [ ] **Step 5：commit**

```bash
git add packages/engine/src/phases/passenger.ts packages/engine/tests/phases/passenger.spec.ts
git commit -m "feat(engine): 抽取 phase passenger 为纯函数"
```

---

### Task 8：phase intent 纯函数（最大头）

把 `src/core/PhysicsEngine.ts:97-243` 的 `computeIntent` 与 `tryResume` 抽出。**关键改动**：

- 增加 `rng: PRNGState` 参数（替换 `Math.random` for dwell/buffer）
- 增加 `events: EngineEvent[]` 参数（替换 `console.error`）
- 增加 `adj: AdjacencyIndex` 参数（替换全局 adjacencyIndex）
- 移除 `RESUME_SPEED` 等常量直接 import（改为 import from constants 模块；引擎内独立常量）

**Files:**
- Create: `packages/engine/src/constants.ts`
- Create: `packages/engine/src/phases/intent.ts`
- Create: `packages/engine/tests/phases/intent.spec.ts`

- [ ] **Step 1：创建 `packages/engine/src/constants.ts`（独立于 src/core/constants.ts，引擎不依赖外部）**

```ts
export const TICKS_PER_SECOND = 60
export const TICKS_PER_MINUTE = 3600

// 列车物理
export const CAR_PITCH = 30
export const RESUME_SPEED = 60
export const PLATFORM_BUFFER_STOP_SAFETY = 60

// 停站
export const DWELL_TIME_MIN = 1800
export const DWELL_TIME_MAX = 3600

// 发车缓冲
export const BUFFER_TIME_MIN = 3600
export const BUFFER_TIME_MAX = 5400
```

- [ ] **Step 2：在 `packages/engine/src/index.ts` 导出常量**

追加：

```ts
export * as ENGINE_CONSTANTS from './constants'
```

- [ ] **Step 3：写测试 `packages/engine/tests/phases/intent.spec.ts`**

```ts
import { describe, expect, it } from 'vitest'
import type { EngineEvent, RailMap, TrainPhysics } from '@engine'
import { buildAdjacency, createPRNG } from '@engine'
import { computeIntent, tryResume } from '@engine/phases/intent'

function makeMap(): RailMap {
  return {
    nodes: {
      a: { id: 'a', x: 0, y: 0, type: 'connector', signalState: 'green' },
      b: { id: 'b', x: 100, y: 0, type: 'connector', signalState: 'green' },
      c: { id: 'c', x: 200, y: 0, type: 'connector', signalState: 'green' },
    },
    edges: {
      e_ab: { id: 'e_ab', fromNode: 'a', toNode: 'b', length: 100, occupiedBy: null },
      e_bc: { id: 'e_bc', fromNode: 'b', toNode: 'c', length: 100, occupiedBy: null, isPlatform: true },
    },
    platforms: [],
  }
}

function makeTrain(over: Partial<TrainPhysics> = {}): TrainPhysics {
  return {
    id: 't1',
    modelType: 'CR400AF',
    isCoupled: false,
    currentEdgeId: 'e_ab',
    position: 0,
    speed: 60,
    state: 'moving',
    direction: 1,
    path: [],
    visitedPath: [],
    ...over,
  }
}

describe('computeIntent — 常规移动', () => {
  it('未到边界，position 前进 speed*dt', () => {
    const map = makeMap()
    const t = makeTrain({ position: 0, speed: 60, direction: 1 })
    const adj = buildAdjacency(map)
    const events: EngineEvent[] = []
    const rng = createPRNG(1)
    computeIntent(t, map, adj, 1, 100, events, rng)
    expect(t.position).toBe(60)
    expect(t.nextMoveIntent).toBeUndefined()
  })

  it('到达边界 + 下一节点 signal=green → 设置 nextMoveIntent', () => {
    const map = makeMap()
    const t = makeTrain({ position: 95, speed: 60, direction: 1 })
    const adj = buildAdjacency(map)
    const events: EngineEvent[] = []
    const rng = createPRNG(1)
    computeIntent(t, map, adj, 1, 100, events, rng)
    expect(t.nextMoveIntent).toBeDefined()
    expect(t.nextMoveIntent!.targetEdgeId).toBe('e_bc')
  })

  it('到达边界 + 下一节点 signal=red → 停车并发出 signal_pass', () => {
    const map = makeMap()
    map.nodes.b!.signalState = 'red'
    const t = makeTrain({ position: 95, speed: 60, direction: 1 })
    const adj = buildAdjacency(map)
    const events: EngineEvent[] = []
    const rng = createPRNG(1)
    computeIntent(t, map, adj, 1, 100, events, rng)
    expect(t.state).toBe('stopped')
    expect(t.position).toBe(100)
    expect(t.speed).toBe(0)
    expect(events.find((e) => e.kind === 'signal_pass')).toBeDefined()
  })

  it('到达 platform 边 + 未服务过 → 停车进入 BOARDING + 发 dwell_start', () => {
    const map = makeMap()
    const t = makeTrain({ currentEdgeId: 'e_bc', position: 95, direction: 1 })
    const adj = buildAdjacency(map)
    const events: EngineEvent[] = []
    const rng = createPRNG(7)
    computeIntent(t, map, adj, 1, 100, events, rng)
    expect(t.state).toBe('stopped')
    expect(t.passengerState).toBe('BOARDING')
    expect(t.lastServicedEdgeId).toBe('e_bc')
    expect(events.find((e) => e.kind === 'dwell_start')).toBeDefined()
  })

  it('platform 已服务过 → 不触发停站', () => {
    const map = makeMap()
    const t = makeTrain({ currentEdgeId: 'e_bc', position: 0, lastServicedEdgeId: 'e_bc' })
    const adj = buildAdjacency(map)
    const events: EngineEvent[] = []
    const rng = createPRNG(1)
    computeIntent(t, map, adj, 1, 100, events, rng)
    expect(t.state).toBe('moving')
  })

  it('当前边 ID 无效 → 停车并发出 invariant_violation', () => {
    const map = makeMap()
    const t = makeTrain({ currentEdgeId: 'no_such_edge' })
    const adj = buildAdjacency(map)
    const events: EngineEvent[] = []
    const rng = createPRNG(1)
    computeIntent(t, map, adj, 1, 100, events, rng)
    expect(t.state).toBe('stopped')
    expect(events.find((e) => e.kind === 'invariant_violation')).toBeDefined()
  })

  it('停站 dwell 时长由 PRNG 决定（确定性）', () => {
    const make = (seed: number) => {
      const map = makeMap()
      const t = makeTrain({ currentEdgeId: 'e_bc', position: 95, direction: 1 })
      const adj = buildAdjacency(map)
      const events: EngineEvent[] = []
      const rng = createPRNG(seed)
      computeIntent(t, map, adj, 1, 100, events, rng)
      return t.boardingTimer
    }
    expect(make(1)).toBe(make(1))
    // 不同 seed 大概率不同（极小概率撞同一桶，但 [1800,3600] 范围内 1 与 2 应不同）
    expect(make(1)).not.toBe(make(2))
  })
})

describe('tryResume', () => {
  it('停站中 boardingTimer 未到不恢复', () => {
    const map = makeMap()
    const t = makeTrain({
      state: 'stopped', speed: 0, currentEdgeId: 'e_ab', position: 100,
      passengerState: 'BOARDING', boardingTimer: 100,
    })
    const adj = buildAdjacency(map)
    tryResume(t, map, adj)
    expect(t.state).toBe('stopped')
  })

  it('passengerState=READY 仍阻止 resume（设计如此：要等 DEPART 命令清除）', () => {
    const map = makeMap()
    const t = makeTrain({
      state: 'stopped', speed: 0, currentEdgeId: 'e_ab', position: 100,
      passengerState: 'READY',
    })
    const adj = buildAdjacency(map)
    tryResume(t, map, adj)
    expect(t.state).toBe('stopped')
  })

  it('无 passengerState + 边界 + 下条边 signal=green → resume', () => {
    const map = makeMap()
    const t = makeTrain({
      state: 'stopped', speed: 0, currentEdgeId: 'e_ab', position: 100,
    })
    const adj = buildAdjacency(map)
    tryResume(t, map, adj)
    expect(t.state).toBe('moving')
    expect(t.speed).toBeGreaterThan(0)
  })

  it('signal=red 时不 resume', () => {
    const map = makeMap()
    map.nodes.b!.signalState = 'red'
    const t = makeTrain({
      state: 'stopped', speed: 0, currentEdgeId: 'e_ab', position: 100,
    })
    const adj = buildAdjacency(map)
    tryResume(t, map, adj)
    expect(t.state).toBe('stopped')
  })
})
```

- [ ] **Step 4：跑测试确认失败**

Run: `npx vitest run packages/engine/tests/phases/intent.spec.ts`
Expected: 11 failed

- [ ] **Step 5：实现 `packages/engine/src/phases/intent.ts`**

```ts
import type { AdjacencyIndex } from '../adjacency'
import { resolveNextEdge } from '../adjacency'
import {
  BUFFER_TIME_MAX,
  BUFFER_TIME_MIN,
  DWELL_TIME_MAX,
  DWELL_TIME_MIN,
  PLATFORM_BUFFER_STOP_SAFETY,
  RESUME_SPEED,
} from '../constants'
import { randomInt, type PRNGState } from '../prng'
import type { EngineEvent, RailMap, TrainPhysics } from '../types'

/**
 * Phase 1：意图计算 + 站台自动停车。
 * mutates train。所有随机性走 rng；所有错误通过 events 反馈，不抛错。
 */
export function computeIntent(
  train: TrainPhysics,
  map: RailMap,
  adj: AdjacencyIndex,
  dt: number,
  tick: number,
  events: EngineEvent[],
  rng: PRNGState,
): void {
  if (train.state !== 'moving' || train.speed === 0) return

  const dir = train.direction
  const distToMove = train.speed * dt
  const tPos = train.position + distToMove * dir
  const currentEdge = map.edges[train.currentEdgeId]

  if (!currentEdge) {
    train.state = 'stopped'
    events.push({
      tick,
      kind: 'invariant_violation',
      rule: 'invalid_current_edge',
      detail: { trainId: train.id, edgeId: train.currentEdgeId },
    })
    return
  }

  const isArriving =
    (dir === 1 && tPos >= currentEdge.length) || (dir === -1 && tPos <= 0)

  if (!isArriving) {
    train.position = tPos
    train.nextMoveIntent = undefined
    return
  }

  const overflow = dir === 1 ? tPos - currentEdge.length : Math.abs(tPos)

  // 站台自动停车
  if (currentEdge.isPlatform && train.lastServicedEdgeId !== train.currentEdgeId) {
    const endNode = map.nodes[currentEdge.toNode]
    const startNode = map.nodes[currentEdge.fromNode]
    const stopPos =
      dir === 1
        ? endNode?.type === 'buffer_stop'
          ? currentEdge.length - PLATFORM_BUFFER_STOP_SAFETY
          : currentEdge.length
        : startNode?.type === 'buffer_stop'
          ? PLATFORM_BUFFER_STOP_SAFETY
          : 0
    train.position = stopPos
    train.speed = 0
    train.state = 'stopped'

    train.passengerState = 'BOARDING'
    train.arrivalTick = tick
    const dwell = randomInt(rng, DWELL_TIME_MIN, DWELL_TIME_MAX - 1)
    train.boardingTimer = dwell
    train.stopDuration = dwell
    train.stopBuffer = randomInt(rng, BUFFER_TIME_MIN, BUFFER_TIME_MAX - 1)
    train.lastServicedEdgeId = train.currentEdgeId

    events.push({
      tick,
      kind: 'dwell_start',
      trainId: train.id,
      edgeId: train.currentEdgeId,
      duration: dwell,
    })
    return
  }

  // path 用尽
  if (!train.path || train.path.length === 0) {
    train.position = dir === 1 ? currentEdge.length : 0
    train.speed = 0
    train.state = 'stopped'
    return
  }

  const nextNodeId = dir === 1 ? currentEdge.toNode : currentEdge.fromNode
  const nextNode = map.nodes[nextNodeId]
  if (!nextNode) {
    train.state = 'stopped'
    events.push({
      tick,
      kind: 'invariant_violation',
      rule: 'invalid_next_node',
      detail: { trainId: train.id, nodeId: nextNodeId },
    })
    return
  }

  const nextEdgeId = resolveNextEdge(nextNode, train.currentEdgeId, adj)

  // 红灯停车
  if (nextNode.signalState === 'red') {
    train.position = dir === 1 ? currentEdge.length : 0
    train.speed = 0
    train.state = 'stopped'
    events.push({
      tick,
      kind: 'signal_pass',
      trainId: train.id,
      nodeId: nextNode.id,
      signalState: 'red',
    })
    return
  }

  if (!nextEdgeId) {
    train.position = dir === 1 ? currentEdge.length : 0
    train.speed = 0
    train.state = 'stopped'
    return
  }

  // 通过绿灯，记下事件
  events.push({
    tick,
    kind: 'signal_pass',
    trainId: train.id,
    nodeId: nextNode.id,
    signalState: 'green',
  })
  train.nextMoveIntent = { targetEdgeId: nextEdgeId, overflowDistance: overflow }
}

/**
 * Phase 1（停车列车的恢复检查）。mutates train。
 */
export function tryResume(
  train: TrainPhysics,
  map: RailMap,
  adj: AdjacencyIndex,
): void {
  const currentEdge = map.edges[train.currentEdgeId]
  if (!currentEdge) return

  // 停站期间不 resume（DEPART 命令通过 DispatchController 清空 passengerState 后才能动）
  if (train.passengerState === 'BOARDING' || train.passengerState === 'READY') return

  const dir = train.direction
  const distToBound = dir === 1 ? currentEdge.length - train.position : train.position
  if (distToBound > 0.1) return

  const nextNodeId = dir === 1 ? currentEdge.toNode : currentEdge.fromNode
  const nextNode = map.nodes[nextNodeId]
  if (!nextNode) return

  const nextEdgeId = resolveNextEdge(nextNode, train.currentEdgeId, adj)
  if (!nextEdgeId) return
  if (nextNode.signalState === 'red') return

  train.state = 'moving'
  train.speed = RESUME_SPEED
}
```

- [ ] **Step 6：跑测试确认通过**

Run: `npx vitest run packages/engine/tests/phases/intent.spec.ts`
Expected: 11 passed

- [ ] **Step 7：commit**

```bash
git add packages/engine/src/constants.ts packages/engine/src/phases/intent.ts packages/engine/src/index.ts packages/engine/tests/phases/intent.spec.ts
git commit -m "feat(engine): 抽取 computeIntent/tryResume 为纯函数 + 引擎独立常量"
```

---

### Task 9：phase collision 纯函数

**Files:**
- Create: `packages/engine/src/phases/collision.ts`
- Create: `packages/engine/tests/phases/collision.spec.ts`

- [ ] **Step 1：写测试 `packages/engine/tests/phases/collision.spec.ts`**

```ts
import { describe, expect, it } from 'vitest'
import type { EngineEvent, TrainPhysics } from '@engine'
import { detectCollisions, getTrainLength } from '@engine/phases/collision'

function train(over: Partial<TrainPhysics>): TrainPhysics {
  return {
    id: 't', modelType: 'CR400AF', isCoupled: false,
    currentEdgeId: 'e1', position: 0, speed: 0, state: 'stopped',
    direction: 1, path: [], visitedPath: [], ...over,
  }
}

describe('detectCollisions', () => {
  it('两列同边重叠 → 发出 collision 事件', () => {
    const t1 = train({ id: 't1', position: 100, currentEdgeId: 'e1', isCoupled: false })
    const t2 = train({ id: 't2', position: 110, currentEdgeId: 'e1', isCoupled: false })
    const events: EngineEvent[] = []
    detectCollisions([t1, t2], 7, events)
    expect(events.find((e) => e.kind === 'collision')).toMatchObject({
      kind: 'collision', trainA: 't1', trainB: 't2', edgeId: 'e1',
    })
  })

  it('不同边的两列不碰撞', () => {
    const t1 = train({ id: 't1', currentEdgeId: 'e1', position: 100 })
    const t2 = train({ id: 't2', currentEdgeId: 'e2', position: 100 })
    const events: EngineEvent[] = []
    detectCollisions([t1, t2], 7, events)
    expect(events.filter((e) => e.kind === 'collision')).toHaveLength(0)
  })

  it('已移交的列车跳过碰撞检测', () => {
    const t1 = train({ id: 't1', position: 100, currentEdgeId: 'e1', isHandedOver: true })
    const t2 = train({ id: 't2', position: 105, currentEdgeId: 'e1' })
    const events: EngineEvent[] = []
    detectCollisions([t1, t2], 7, events)
    expect(events.filter((e) => e.kind === 'collision')).toHaveLength(0)
  })

  it('isCoupled=true → 列车长度翻倍', () => {
    expect(getTrainLength({ ...train({}), isCoupled: false })).toBe(8 * 30)
    expect(getTrainLength({ ...train({}), isCoupled: true })).toBe(16 * 30)
  })

  it('间距大于双方车长之和 → 不碰撞', () => {
    const t1 = train({ id: 't1', position: 100, currentEdgeId: 'e1' })
    const t2 = train({ id: 't2', position: 500, currentEdgeId: 'e1' })
    const events: EngineEvent[] = []
    detectCollisions([t1, t2], 7, events)
    expect(events.filter((e) => e.kind === 'collision')).toHaveLength(0)
  })
})
```

- [ ] **Step 2：跑测试确认失败**

Run: `npx vitest run packages/engine/tests/phases/collision.spec.ts`
Expected: 5 failed

- [ ] **Step 3：实现 `packages/engine/src/phases/collision.ts`**

```ts
import { CAR_PITCH } from '../constants'
import type { EngineEvent, TrainPhysics } from '../types'

export function getTrainLength(train: TrainPhysics): number {
  const carCount = train.isCoupled ? 16 : 8
  return carCount * CAR_PITCH
}

function overlap(h1: number, t1: number, h2: number, t2: number): boolean {
  // train head 在前进方向的端点；tail = head - length。
  // h1/t1/h2/t2 都是同一坐标系下的标量；位置区间 [t,h]。
  const completelyBefore = h1 < t2
  const completelyAfter = t1 > h2
  return !(completelyBefore || completelyAfter)
}

/**
 * Phase 1.5：物理碰撞检测。
 * 同一边上两列重叠 → 发 collision 事件（不抛错；step 由调用方统一处理后果）。
 */
export function detectCollisions(
  trains: TrainPhysics[],
  tick: number,
  events: EngineEvent[],
): void {
  for (let i = 0; i < trains.length; i++) {
    for (let j = i + 1; j < trains.length; j++) {
      const a = trains[i]!
      const b = trains[j]!
      if (a.isHandedOver || b.isHandedOver) continue
      if (a.currentEdgeId !== b.currentEdgeId) continue
      const aHead = a.position
      const aTail = a.position - getTrainLength(a)
      const bHead = b.position
      const bTail = b.position - getTrainLength(b)
      if (overlap(aHead, aTail, bHead, bTail)) {
        events.push({
          tick, kind: 'collision', trainA: a.id, trainB: b.id, edgeId: a.currentEdgeId,
        })
      }
    }
  }
}
```

- [ ] **Step 4：跑测试确认通过**

Run: `npx vitest run packages/engine/tests/phases/collision.spec.ts`
Expected: 5 passed

- [ ] **Step 5：commit**

```bash
git add packages/engine/src/phases/collision.ts packages/engine/tests/phases/collision.spec.ts
git commit -m "feat(engine): 抽取 detectCollisions 为纯函数"
```

---

### Task 10：phase conflict 纯函数

**Files:**
- Create: `packages/engine/src/phases/conflict.ts`
- Create: `packages/engine/tests/phases/conflict.spec.ts`

- [ ] **Step 1：写测试 `packages/engine/tests/phases/conflict.spec.ts`**

```ts
import { describe, expect, it } from 'vitest'
import type { EngineEvent, TrainPhysics } from '@engine'
import { resolveConflicts } from '@engine/phases/conflict'

function trainWithIntent(id: string, target: string): TrainPhysics {
  return {
    id, modelType: 'CR400AF', isCoupled: false,
    currentEdgeId: 'src', position: 50, speed: 60, state: 'moving',
    direction: 1, path: [], visitedPath: [],
    nextMoveIntent: { targetEdgeId: target, overflowDistance: 5 },
  }
}

describe('resolveConflicts', () => {
  it('两列声明同一目标边 → 发出 collision 事件', () => {
    const a = trainWithIntent('A', 'e_target')
    const b = trainWithIntent('B', 'e_target')
    const events: EngineEvent[] = []
    resolveConflicts([a, b], 100, events)
    expect(events.find((e) => e.kind === 'collision')).toMatchObject({
      kind: 'collision', trainA: 'A', trainB: 'B', edgeId: 'e_target',
    })
  })

  it('目标边不同 → 无冲突', () => {
    const a = trainWithIntent('A', 'e1')
    const b = trainWithIntent('B', 'e2')
    const events: EngineEvent[] = []
    resolveConflicts([a, b], 100, events)
    expect(events.filter((e) => e.kind === 'collision')).toHaveLength(0)
  })

  it('无意图的列车不参与', () => {
    const a = trainWithIntent('A', 'e1')
    a.nextMoveIntent = undefined
    const b = trainWithIntent('B', 'e1')
    const events: EngineEvent[] = []
    resolveConflicts([a, b], 100, events)
    expect(events.filter((e) => e.kind === 'collision')).toHaveLength(0)
  })
})
```

- [ ] **Step 2：跑测试确认失败**

Run: `npx vitest run packages/engine/tests/phases/conflict.spec.ts`
Expected: 3 failed

- [ ] **Step 3：实现 `packages/engine/src/phases/conflict.ts`**

```ts
import type { EngineEvent, TrainPhysics } from '../types'

/**
 * Phase 2：解决"同 tick 抢占同一目标边"的竞争条件。
 * 调用方持有 claimsMap 复用以减少分配；这里每次新建（性能微开销，换简洁）。
 */
export function resolveConflicts(
  trains: TrainPhysics[],
  tick: number,
  events: EngineEvent[],
): void {
  const claims = new Map<string, string>()
  for (const train of trains) {
    if (!train.nextMoveIntent) continue
    const target = train.nextMoveIntent.targetEdgeId
    if (claims.has(target)) {
      events.push({
        tick, kind: 'collision',
        trainA: train.id,
        trainB: claims.get(target)!,
        edgeId: target,
      })
    }
    claims.set(target, train.id)
  }
}
```

- [ ] **Step 4：跑测试确认通过**

Run: `npx vitest run packages/engine/tests/phases/conflict.spec.ts`
Expected: 3 passed

- [ ] **Step 5：commit**

```bash
git add packages/engine/src/phases/conflict.ts packages/engine/tests/phases/conflict.spec.ts
git commit -m "feat(engine): 抽取 resolveConflicts 为纯函数"
```

---

### Task 11：phase commit 纯函数

**Files:**
- Create: `packages/engine/src/phases/commit.ts`
- Create: `packages/engine/tests/phases/commit.spec.ts`

- [ ] **Step 1：写测试 `packages/engine/tests/phases/commit.spec.ts`**

```ts
import { describe, expect, it } from 'vitest'
import type { EngineEvent, RailMap, TrainPhysics } from '@engine'
import { commitMoves } from '@engine/phases/commit'

function makeMap(): RailMap {
  return {
    nodes: {
      a: { id: 'a', x: 0, y: 0, type: 'connector' },
      b: { id: 'b', x: 1, y: 0, type: 'connector' },
      c: { id: 'c', x: 2, y: 0, type: 'connector' },
    },
    edges: {
      e_ab: { id: 'e_ab', fromNode: 'a', toNode: 'b', length: 100, occupiedBy: null },
      e_bc: { id: 'e_bc', fromNode: 'b', toNode: 'c', length: 100, occupiedBy: null },
    },
    platforms: [],
  }
}

function makeTrain(over: Partial<TrainPhysics> = {}): TrainPhysics {
  return {
    id: 't1', modelType: 'CR400AF', isCoupled: false,
    currentEdgeId: 'e_ab', position: 100, speed: 60, state: 'moving',
    direction: 1, path: ['e_bc'], visitedPath: [],
    nextMoveIntent: { targetEdgeId: 'e_bc', overflowDistance: 5 },
    ...over,
  }
}

describe('commitMoves', () => {
  it('迁移到新边 + 释放旧边占用 + 占用新边', () => {
    const map = makeMap()
    map.edges.e_ab!.occupiedBy = 't1'
    const t = makeTrain()
    const events: EngineEvent[] = []
    commitMoves([t], map, 100, events)
    expect(t.currentEdgeId).toBe('e_bc')
    expect(map.edges.e_ab!.occupiedBy).toBeNull()
    expect(map.edges.e_bc!.occupiedBy).toBe('t1')
    expect(t.position).toBe(5)
    expect(t.nextMoveIntent).toBeUndefined()
  })

  it('从 b.outgoing(e_bc) 进入 e_bc → direction=1', () => {
    const map = makeMap()
    const t = makeTrain()
    const events: EngineEvent[] = []
    commitMoves([t], map, 100, events)
    expect(t.direction).toBe(1)
  })

  it('反向到达节点 → 自动设 direction=-1', () => {
    const map: RailMap = {
      nodes: {
        a: { id: 'a', x: 0, y: 0, type: 'connector' },
        b: { id: 'b', x: 1, y: 0, type: 'connector' },
        c: { id: 'c', x: 2, y: 0, type: 'connector' },
      },
      edges: {
        e_ab: { id: 'e_ab', fromNode: 'a', toNode: 'b', length: 100, occupiedBy: null },
        // e_cb 是 c→b，列车在 b 反向到达 c → 进入 e_cb 应 direction=-1
        e_cb: { id: 'e_cb', fromNode: 'c', toNode: 'b', length: 100, occupiedBy: null },
      },
      platforms: [],
    }
    const t = makeTrain({
      currentEdgeId: 'e_ab', direction: 1, position: 100,
      path: ['e_cb'],
      nextMoveIntent: { targetEdgeId: 'e_cb', overflowDistance: 5 },
    })
    const events: EngineEvent[] = []
    commitMoves([t], map, 100, events)
    expect(t.direction).toBe(-1)
    expect(t.position).toBe(95) // length 100 - overflow 5
  })

  it('发出 enter_edge 事件', () => {
    const map = makeMap()
    const t = makeTrain()
    const events: EngineEvent[] = []
    commitMoves([t], map, 100, events)
    expect(events.find((e) => e.kind === 'enter_edge')).toMatchObject({
      from: 'e_ab', to: 'e_bc', trainId: 't1',
    })
  })

  it('visitedPath 记录历史，最多 20 条', () => {
    const map = makeMap()
    const t = makeTrain({ visitedPath: new Array(20).fill('old') })
    const events: EngineEvent[] = []
    commitMoves([t], map, 100, events)
    expect(t.visitedPath).toHaveLength(20)
    expect(t.visitedPath[0]).toBe('e_ab')
  })

  it('path 顶端正是 nextEdge → 消费 path', () => {
    const map = makeMap()
    const t = makeTrain({ path: ['e_bc', 'e_cd'] })
    const events: EngineEvent[] = []
    commitMoves([t], map, 100, events)
    expect(t.path).toEqual(['e_cd'])
  })

  it('目标边不存在 → 停车并发 invariant_violation', () => {
    const map = makeMap()
    const t = makeTrain({
      nextMoveIntent: { targetEdgeId: 'no_such', overflowDistance: 5 },
    })
    const events: EngineEvent[] = []
    commitMoves([t], map, 100, events)
    expect(t.state).toBe('stopped')
    expect(events.find((e) => e.kind === 'invariant_violation')).toBeDefined()
  })

  it('无意图 → 不变更', () => {
    const map = makeMap()
    const t = makeTrain({ nextMoveIntent: undefined })
    const events: EngineEvent[] = []
    commitMoves([t], map, 100, events)
    expect(t.currentEdgeId).toBe('e_ab')
  })
})
```

- [ ] **Step 2：跑测试确认失败**

Run: `npx vitest run packages/engine/tests/phases/commit.spec.ts`
Expected: 8 failed

- [ ] **Step 3：实现 `packages/engine/src/phases/commit.ts`**

```ts
import type { EngineEvent, RailMap, TrainPhysics } from '../types'

/**
 * Phase 3：把意图提交为状态变化。
 * - 释放旧边占用；占用新边
 * - 根据新边拓扑自动设 direction
 * - 维护 visitedPath（保留最近 20 段，用于尾部渲染）
 * - 消费 path[0] 当且仅当其匹配 nextEdgeId
 */
export function commitMoves(
  trains: TrainPhysics[],
  map: RailMap,
  tick: number,
  events: EngineEvent[],
): void {
  for (const train of trains) {
    if (!train.nextMoveIntent) continue

    const oldEdgeId = train.currentEdgeId
    const oldEdge = map.edges[oldEdgeId]
    if (oldEdge && oldEdge.occupiedBy === train.id) {
      oldEdge.occupiedBy = null
    }

    train.visitedPath = train.visitedPath ?? []
    train.visitedPath.unshift(oldEdgeId)
    if (train.visitedPath.length > 20) train.visitedPath.pop()

    const currentEdge = map.edges[oldEdgeId]
    if (!currentEdge) {
      train.nextMoveIntent = undefined
      continue
    }

    const arrivalNodeId =
      train.direction === 1 ? currentEdge.toNode : currentEdge.fromNode

    const nextEdgeId = train.nextMoveIntent.targetEdgeId
    const nextEdge = map.edges[nextEdgeId]
    if (!nextEdge) {
      train.state = 'stopped'
      train.nextMoveIntent = undefined
      events.push({
        tick, kind: 'invariant_violation', rule: 'commit_missing_next_edge',
        detail: { trainId: train.id, edgeId: nextEdgeId },
      })
      continue
    }

    nextEdge.occupiedBy = train.id

    if (nextEdge.fromNode === arrivalNodeId) {
      train.direction = 1
      train.position = train.nextMoveIntent.overflowDistance
    } else if (nextEdge.toNode === arrivalNodeId) {
      train.direction = -1
      train.position = nextEdge.length - train.nextMoveIntent.overflowDistance
    } else {
      events.push({
        tick, kind: 'invariant_violation', rule: 'edge_discontinuity',
        detail: { trainId: train.id, from: oldEdgeId, to: nextEdgeId },
      })
      train.position = train.nextMoveIntent.overflowDistance
    }

    train.currentEdgeId = nextEdgeId

    if (train.path && train.path.length > 0 && train.path[0] === nextEdgeId) {
      train.path.shift()
    }

    events.push({
      tick, kind: 'enter_edge', trainId: train.id,
      from: oldEdgeId, to: nextEdgeId,
    })

    train.nextMoveIntent = undefined
  }
}
```

- [ ] **Step 4：跑测试确认通过**

Run: `npx vitest run packages/engine/tests/phases/commit.spec.ts`
Expected: 8 passed

- [ ] **Step 5：commit**

```bash
git add packages/engine/src/phases/commit.ts packages/engine/tests/phases/commit.spec.ts
git commit -m "feat(engine): 抽取 commitMoves 为纯函数"
```

---

### Task 12：step() 主入口组装

**Files:**
- Create: `packages/engine/src/step.ts`
- Create: `packages/engine/tests/step.spec.ts`
- Modify: `packages/engine/src/index.ts`

- [ ] **Step 1：写测试 `packages/engine/tests/step.spec.ts`（黄金路径）**

```ts
import { describe, expect, it } from 'vitest'
import type { EngineState, RailMap, TrainPhysics } from '@engine'
import { createPRNG, step } from '@engine'

function makeRailMap(): RailMap {
  return {
    nodes: {
      a: { id: 'a', x: 0, y: 0, type: 'connector', signalState: 'green' },
      b: { id: 'b', x: 1, y: 0, type: 'connector', signalState: 'green' },
      c: { id: 'c', x: 2, y: 0, type: 'connector', signalState: 'green' },
    },
    edges: {
      e_ab: { id: 'e_ab', fromNode: 'a', toNode: 'b', length: 60, occupiedBy: null },
      e_bc: { id: 'e_bc', fromNode: 'b', toNode: 'c', length: 60, occupiedBy: null },
    },
    platforms: [],
  }
}

function makeTrain(): TrainPhysics {
  return {
    id: 't1', modelType: 'CR400AF', isCoupled: false,
    currentEdgeId: 'e_ab', position: 0, speed: 60, state: 'moving',
    direction: 1, path: ['e_bc'], visitedPath: [],
  }
}

function initialState(): EngineState {
  const map = makeRailMap()
  map.edges.e_ab!.occupiedBy = 't1'
  return {
    tick: 0,
    trains: [makeTrain()],
    railMap: map,
    rng: createPRNG(1),
  }
}

describe('step', () => {
  it('一个 tick 推进 tick 计数', () => {
    const s = initialState()
    const r = step(s, [])
    expect(r.next.tick).toBe(1)
  })

  it('黄金路径：列车经 e_ab → e_bc，最终在 e_bc', () => {
    let s = initialState()
    // dt=1/60, speed=60 → 每 tick 前进 1 单位；e_ab 长度 60 → 60 个 tick 到边界
    for (let i = 0; i < 65; i++) {
      const r = step(s, [])
      s = r.next
    }
    expect(s.trains[0]!.currentEdgeId).toBe('e_bc')
  })

  it('确定性：相同 seed + 相同输入 → 相同结果', () => {
    let a = initialState()
    let b = initialState()
    for (let i = 0; i < 200; i++) {
      a = step(a, []).next
      b = step(b, []).next
    }
    expect(a.trains[0]!.position).toBe(b.trains[0]!.position)
    expect(a.trains[0]!.currentEdgeId).toBe(b.trains[0]!.currentEdgeId)
    expect(a.rng.state).toBe(b.rng.state)
  })

  it('signal_toggle 输入切换信号灯', () => {
    const s = initialState()
    const r = step(s, [
      { tick: 0, type: 'signal_toggle', payload: { nodeId: 'b' } },
    ])
    expect(r.next.railMap.nodes.b!.signalState).toBe('red')
    expect(r.events.find((e) => e.kind === 'signal_toggled')).toBeDefined()
  })

  it('switch_toggle 输入循环 switchState', () => {
    const s = initialState()
    s.railMap.nodes.b!.type = 'switch'
    s.railMap.nodes.b!.switchState = 0
    s.railMap.edges.e_bc2 = {
      id: 'e_bc2', fromNode: 'b', toNode: 'c', length: 60, occupiedBy: null,
    }
    const r = step(s, [
      { tick: 0, type: 'switch_toggle', payload: { nodeId: 'b' } },
    ])
    expect(r.next.railMap.nodes.b!.switchState).toBe(1)
  })

  it('events 中包含 enter_edge 当列车跨边时', () => {
    let s = initialState()
    let crossed = false
    for (let i = 0; i < 70 && !crossed; i++) {
      const r = step(s, [])
      s = r.next
      if (r.events.some((e) => e.kind === 'enter_edge')) crossed = true
    }
    expect(crossed).toBe(true)
  })
})
```

- [ ] **Step 2：跑测试确认失败**

Run: `npx vitest run packages/engine/tests/step.spec.ts`
Expected: 6 failed

- [ ] **Step 3：实现 `packages/engine/src/step.ts`**

```ts
import { buildAdjacency } from './adjacency'
import { TICKS_PER_SECOND } from './constants'
import { detectCollisions } from './phases/collision'
import { commitMoves } from './phases/commit'
import { resolveConflicts } from './phases/conflict'
import { computeIntent, tryResume } from './phases/intent'
import { handlePassenger } from './phases/passenger'
import { clonePRNG } from './prng'
import type {
  EngineEvent,
  EngineInput,
  EngineState,
  Invariant,
  StepResult,
  TrainPhysics,
} from './types'

const FIXED_DT = 1 / TICKS_PER_SECOND

/**
 * 引擎主入口。
 * - 不假设调用频率；FIXED_DT 写死为 1/60 秒。倍速由调用方多次调用 step 实现。
 * - 输入：state（不会被修改）+ inputs（本 tick 玩家行为）
 * - 输出：next（深拷贝；mutates 仅发生在内部副本）+ events + violations
 *
 * 不变量校验在 Task 22 接入。本任务先返回空 violations。
 */
export function step(state: EngineState, inputs: EngineInput[]): StepResult {
  const next = cloneState(state)
  const events: EngineEvent[] = []
  const violations: Invariant[] = []
  const tick = next.tick

  applyInputs(next, inputs, events)

  const adj = buildAdjacency(next.railMap)

  for (const train of next.trains) {
    handlePassenger(train, tick, events)
  }
  for (const train of next.trains) {
    if (train.state === 'moving') {
      computeIntent(train, next.railMap, adj, FIXED_DT, tick, events, next.rng)
    } else {
      tryResume(train, next.railMap, adj)
    }
  }
  detectCollisions(next.trains, tick, events)
  resolveConflicts(next.trains, tick, events)
  commitMoves(next.trains, next.railMap, tick, events)

  next.tick = tick + 1
  return { next, events, violations }
}

function cloneState(state: EngineState): EngineState {
  return {
    tick: state.tick,
    trains: state.trains.map((t) => ({ ...t, path: [...t.path], visitedPath: [...t.visitedPath] })),
    railMap: cloneRailMap(state.railMap),
    rng: clonePRNG(state.rng),
  }
}

function cloneRailMap(map: EngineState['railMap']): EngineState['railMap'] {
  const nodes: typeof map.nodes = {}
  for (const k of Object.keys(map.nodes)) nodes[k] = { ...map.nodes[k]! }
  const edges: typeof map.edges = {}
  for (const k of Object.keys(map.edges)) edges[k] = { ...map.edges[k]! }
  return { ...map, nodes, edges }
}

function applyInputs(state: EngineState, inputs: EngineInput[], events: EngineEvent[]): void {
  for (const input of inputs) {
    if (input.type === 'switch_toggle') {
      const node = state.railMap.nodes[input.payload.nodeId]
      if (!node) continue
      const outCount = countOutgoingEdges(state.railMap, node.id)
      const cur = node.switchState ?? 0
      const newState = outCount > 0 ? (cur + 1) % outCount : cur
      node.switchState = newState
      events.push({ tick: state.tick, kind: 'switch_toggled', nodeId: node.id, newState })
    } else if (input.type === 'signal_toggle') {
      const node = state.railMap.nodes[input.payload.nodeId]
      if (!node) continue
      node.signalState = node.signalState === 'green' ? 'red' : 'green'
      events.push({
        tick: state.tick, kind: 'signal_toggled', nodeId: node.id,
        newState: node.signalState,
      })
    } else if (input.type === 'admit') {
      const spec = input.payload.trainSpec
      const train: TrainPhysics = {
        id: spec.id,
        modelType: spec.modelType,
        isCoupled: spec.isCoupled,
        currentEdgeId: spec.currentEdgeId,
        position: 0,
        speed: spec.speed,
        state: 'moving',
        direction: spec.direction,
        path: [...spec.path],
        visitedPath: [],
        scheduleEntryId: spec.scheduleEntryId,
        scheduledArriveTick: spec.scheduledArriveTick,
      }
      state.trains.push(train)
      const edge = state.railMap.edges[spec.currentEdgeId]
      if (edge) edge.occupiedBy = spec.id
      events.push({
        tick: state.tick, kind: 'admit', trainId: spec.id, edgeId: spec.currentEdgeId,
      })
    } else if (input.type === 'depart') {
      const train = state.trains.find((t) => t.id === input.payload.trainId)
      if (!train) continue
      train.passengerState = undefined
      train.boardingTimer = 0
      events.push({
        tick: state.tick, kind: 'depart', trainId: train.id, strategy: 'standard',
      })
    } else if (input.type === 'stop') {
      const train = state.trains.find((t) => t.id === input.payload.trainId)
      if (!train) continue
      train.speed = 0
      train.state = 'stopped'
      events.push({ tick: state.tick, kind: 'stop_command', trainId: train.id })
    }
  }
}

function countOutgoingEdges(map: EngineState['railMap'], nodeId: string): number {
  let count = 0
  for (const e of Object.values(map.edges)) if (e.fromNode === nodeId) count++
  return count
}
```

- [ ] **Step 4：在 `packages/engine/src/index.ts` 导出 step**

追加：

```ts
export { step } from './step'
```

- [ ] **Step 5：跑全部引擎测试确认通过**

Run: `npx vitest run packages/engine/`
Expected: 全部 passed

- [ ] **Step 6：commit**

```bash
git add packages/engine/src/step.ts packages/engine/src/index.ts packages/engine/tests/step.spec.ts
git commit -m "feat(engine): 实现 step() 主入口组装 5 个 phase + 输入处理"
```

---

## Phase E：切换运行时到新引擎

### Task 13：让 App.vue 调用 step() 替代旧 PhysicsEngine.update

这一步是**重构性切换**。先在 App.vue 内部维护一个 `engineState`，每帧调 `step()` 拿到 `next` 后再把 `engineState` 设为 `next`。Vue UI 层观察一个 `shallowRef<EngineState>`。

**Files:**
- Modify: `src/App.vue`
- Create: `src/game/types.ts`

- [ ] **Step 1：创建 `src/game/types.ts`，重新导出 engine 类型用于 UI 层**

```ts
export type {
  EngineState,
  EngineInput,
  EngineEvent,
  TrainPhysics,
  RailMap,
  RailNode,
  RailEdge,
  PlatformZone,
  TrainSpawnSpec,
} from '@engine'
```

- [ ] **Step 2：在 `src/App.vue` 顶部 script setup 中替换 import**

把原本对 `./core/PhysicsEngine` 的依赖删掉，改为：

```ts
import { step, createPRNG } from '@engine'
import type { EngineInput, EngineState } from '@engine'
```

- [ ] **Step 3：把 `map = reactive<RailMap>(...)` 与 `trains = reactive<TrainPhysics[]>([])` 替换为 `engineState = shallowRef<EngineState>(...)`**

```ts
const engineState = shallowRef<EngineState>({
  tick: 0,
  trains: [],
  railMap: { nodes: {}, edges: {}, platforms: [] },
  rng: createPRNG(Date.now() & 0xffffffff),
})

const pendingInputs = ref<EngineInput[]>([])
```

- [ ] **Step 4：把 `tick = ref(0)` 删除（用 engineState.value.tick）**

把 `const tick = ref(0)` 删除；后续 `tick.value` 全部替换为 `engineState.value.tick`。

- [ ] **Step 5：替换 loop() 中的物理推进**

原 `App.vue` `loop()` 中的：

```ts
PhysicsEngine.update(trains, map, FIXED_STEP, tick.value)
tick.value++
```

替换为：

```ts
const inputs = pendingInputs.value
pendingInputs.value = []
const result = step(engineState.value, inputs)
engineState.value = result.next
emitEvents(result.events)   // 由 EventBus 接管，Task 23 接入
```

- [ ] **Step 6：把 `handleStationSelect` 中加载车站时的赋值改为构造新 EngineState**

```ts
function handleStationSelect(config: StationConfig) {
  // ...
  engineState.value = {
    tick: 0,
    trains: [],
    railMap: cloneRailMap(config.railMap),
    rng: createPRNG(Math.floor(Math.random() * 0xffffffff)),
  }
  // ...
}

function cloneRailMap(src: RailMap): RailMap {
  return JSON.parse(JSON.stringify(src))
}
```

（注：本步保留 `Math.random()` 一处，仅用于"开局生成 seed"。Task 14 会把这一处也走 PRNG，但 PRNG 自身的 seed 必须有种子来源——我们就用 `Date.now()`，这是允许的非游戏随机源。）

- [ ] **Step 7：把所有读取 `map.xxx` / `trains` 的位置改为 `engineState.value.railMap.xxx` / `engineState.value.trains`**

逐处替换：

- `map.nodes` → `engineState.value.railMap.nodes`
- `map.edges` → `engineState.value.railMap.edges`
- `trains.find(...)` → `engineState.value.trains.find(...)`
- `trains.push(...)` 改为通过 `pendingInputs.value.push({ type: 'admit', ... })`
- `trains.splice(...)` 移除——出站列车清理在 Task 21（admit/depart/remove）里通过 step 内置 + 一个 helper 完成

- [ ] **Step 8：把按钮事件改为入队 inputs**

> **时序说明**：本任务暂时**保留** `spawnTrainIntoMap` 内联生成 `TrainPhysics` 并直接 `engineState.value.trains.push(...)` 的形态（mutate 副本，因 cloneState 后 trains 是新数组）；ADMIT 不走 `inputRecorder`。Task 26 抽出 TrainSpawner 后再切到 inputRecorder.record 路径。这样 Task 13 只做"step() 替换 + Vue 接线"一件事。

`handleAction` 中 DEPART/STOP 改为 push 到 `pendingInputs`，ADMIT 暂保持原行为：

```ts
function handleAction(action: TrainAction) {
  if (action === 'ADMIT') {
    // 之前 spawnTrainIntoMap(...) 直接修改 trains
    // 改为构造 TrainSpawnSpec 后入队 admit
    const spec = buildAdmitSpec(...)
    if (spec) pendingInputs.value.push({ tick: engineState.value.tick, type: 'admit', payload: { trainSpec: spec } })
  }
  if (action === 'DEPART') {
    if (!selectedTrainId.value) return
    pendingInputs.value.push({
      tick: engineState.value.tick,
      type: 'depart',
      payload: { trainId: selectedTrainId.value, targetExitNodeId: getExitNodeId() },
    })
  }
  if (action === 'STOP') {
    if (!selectedTrainId.value) return
    pendingInputs.value.push({
      tick: engineState.value.tick,
      type: 'stop',
      payload: { trainId: selectedTrainId.value },
    })
  }
}
```

`toggleSwitchGroup` / 信号灯切换按钮 / 键盘 1-9 / Q-P 全部改为 push `switch_toggle` / `signal_toggle` 输入。

- [ ] **Step 9：修复编译错误 + 跑现有测试**

Run: `npm run type-check`
Expected: 0 错误（如有错误逐条修复，直到通过）

Run: `npx vitest run`
Expected: 现有测试 pass（ScheduleManager、useDispatchLog、引擎所有测试）

- [ ] **Step 10：手动冒烟（启动 dev 跑两局看列车移动正常）**

Run: `npm run dev`
打开 `http://127.0.0.1:5173`，选小型车站，按 ADMIT 接入一辆车，目视确认移动正常、能停站、能出站。

- [ ] **Step 11：commit**

```bash
git add src/App.vue src/game/types.ts
git commit -m "refactor: App.vue 切换到 packages/engine 的 step() 入口"
```

---

### Task 14：把 ScheduleManager 与 spawn 逻辑接入 PRNG

`ScheduleManager` 内部仍然用 `Math.random`，这违反"确定性"原则。本任务把 `ScheduleManager` 改造为**接受外部 PRNG**。

**Files:**
- Modify: `src/core/ScheduleManager.ts`
- Modify: `src/__tests__/ScheduleManager.spec.ts`
- Modify: `src/App.vue`

- [ ] **Step 1：在 `ScheduleManager.ts` 顶部 import PRNG**

```ts
import type { PRNGState } from '@engine'
import { randomChance, randomChoice, randomFloat, randomInt } from '@engine'
```

- [ ] **Step 2：构造函数加 `rng: PRNGState` 参数**

```ts
constructor(
  config: ScheduleConfig,
  difficulty: number,
  startTick: number,
  gameStartTimeOffsetTicks: number,
  rng: PRNGState,
) {
  this.config = config
  this.difficulty = difficulty
  this.gameStartTimeOffsetTicks = gameStartTimeOffsetTicks
  this.lastGeneratedTick = startTick
  this.rng = rng
  this.seedInitialWaiting(startTick)
  this.generateUpTo(startTick + FUTURE_WINDOW)
}

private rng: PRNGState
```

- [ ] **Step 3：替换 `Math.random()` 全部 6 处**

| 位置（按 grep 结果） | 替换 |
|---|---|
| `getInterval` 第 99 行 `Math.random()` | `randomFloat(this.rng)` |
| `createEntry` 第 109 行 `Math.random() < this.config.directionRatio` | `randomChance(this.rng, this.config.directionRatio)` |
| 第 115 行 `Math.random() > 0.5 ? 'G' : 'D'` | `randomChance(this.rng, 0.5) ? 'G' : 'D'` |
| 第 118 行 `TRAIN_MODELS[Math.floor(Math.random() * TRAIN_MODELS.length)]` | `randomChoice(this.rng, TRAIN_MODELS)` |
| 第 120 行 `DWELL_TIME_MIN + Math.floor(Math.random() * (DWELL_TIME_MAX - DWELL_TIME_MIN + 1))` | `randomInt(this.rng, DWELL_TIME_MIN, DWELL_TIME_MAX)` |
| 第 123 行 `Math.floor(-maxInitialDelay + Math.random() * 2 * maxInitialDelay)` | `randomInt(this.rng, -maxInitialDelay, maxInitialDelay)` |
| 第 146 行 `Math.random() * totalWeight` | `randomFloat(this.rng) * totalWeight` |
| 第 153 行 `lines[Math.floor(Math.random() * lines.length)]` | `randomChoice(this.rng, lines)` |
| 第 167 行 `Math.random() < DELAY_DRIFT_PROBABILITY` | `randomChance(this.rng, DELAY_DRIFT_PROBABILITY)` |
| 第 168 行 `Math.floor((Math.random() * 2 - 1) * DELAY_DRIFT_AMPLITUDE * this.difficulty)` | `randomInt(this.rng, -amp, amp)` （amp = `DELAY_DRIFT_AMPLITUDE * this.difficulty`） |

- [ ] **Step 4：更新 `src/__tests__/ScheduleManager.spec.ts`**

每个 `new ScheduleManager(...)` 末尾加 `, createPRNG(seed)` 参数。导入 `createPRNG`。

```ts
import { createPRNG } from '@engine'

const sm = new ScheduleManager(config, 1, 0, 0, createPRNG(42))
```

- [ ] **Step 5：跑测试确认通过**

Run: `npx vitest run src/__tests__/ScheduleManager.spec.ts`
Expected: 全部 pass

- [ ] **Step 6：在 `App.vue` 替换 `spawnTrainIntoMap` 中的 Math.random**

把 779 行 `Math.floor(Math.random() * 4) + 1` 与 812 行 `Math.random() < 0.2` 替换：先在 App.vue 顶部维护一个独立的 UI-PRNG（不可与引擎 rng 共享，避免渲染影响物理）：

```ts
import { createPRNG, randomChance, randomInt } from '@engine'

// UI 层独立 PRNG，仅用于不影响引擎物理状态的随机选择（站台号、车型耦合）
const uiRng = createPRNG(Date.now() & 0xffffffff)
```

替换：

```ts
const platformNum = randomInt(uiRng, 1, 4)
// ...
isCoupled: randomChance(uiRng, 0.2),
```

- [ ] **Step 7：替换 `generateRandomStartTime`（仍允许使用 Date.now 生成 seed，但内部用 PRNG）**

```ts
function generateRandomStartTime() {
  const seedRng = createPRNG((Date.now() ^ 0xdeadbeef) >>> 0)
  return {
    hours: randomInt(seedRng, 7, 19),
    minutes: randomInt(seedRng, 0, 59),
    seconds: randomInt(seedRng, 0, 59),
  }
}
```

- [ ] **Step 8：把 `handleStationSelect` 创建 ScheduleManager 时传入 PRNG**

```ts
const scheduleRng = createPRNG((Date.now() ^ 0xfeedface) >>> 0)
scheduleManager.value = new ScheduleManager(
  config.scheduleConfig,
  config.difficulty,
  0,
  startTimeOffsetTicks,
  scheduleRng,
)
```

- [ ] **Step 9：grep 确认 `Math.random` 仅剩"seed 来源"位置**

Run: `npx grep "Math\\.random" src packages` 或者使用项目搜索
Expected: 仅出现在 `createPRNG((Date.now() ^ ...) >>> 0)` 类似位置（即 PRNG 自身的 seed 来源），引擎逻辑零出现

- [ ] **Step 10：跑全部测试 + 类型检查**

Run: `npm run type-check`
Run: `npx vitest run`
Expected: 全 pass

- [ ] **Step 11：commit**

```bash
git add src/core/ScheduleManager.ts src/__tests__/ScheduleManager.spec.ts src/App.vue
git commit -m "refactor: ScheduleManager 与 spawn 逻辑接入 PRNG，消除 Math.random 在游戏逻辑中的使用"
```

---

## Phase F：不变量断言

### Task 15：assertInvariants 框架

**Files:**
- Create: `packages/engine/src/invariants.ts`
- Create: `packages/engine/tests/invariants.spec.ts`
- Modify: `packages/engine/src/step.ts`
- Modify: `packages/engine/src/index.ts`

- [ ] **Step 1：写测试 `packages/engine/tests/invariants.spec.ts`（仅框架部分）**

```ts
import { describe, expect, it } from 'vitest'
import type { EngineState } from '@engine'
import { createPRNG } from '@engine'
import { assertInvariants, isDevMode, setDevMode } from '@engine/invariants'

function emptyState(): EngineState {
  return {
    tick: 0, trains: [], rng: createPRNG(1),
    railMap: { nodes: {}, edges: {}, platforms: [] },
  }
}

describe('assertInvariants 框架', () => {
  it('空状态无违反', () => {
    expect(assertInvariants(emptyState())).toEqual([])
  })

  it('setDevMode/isDevMode 切换可读', () => {
    const before = isDevMode()
    setDevMode(true)
    expect(isDevMode()).toBe(true)
    setDevMode(false)
    expect(isDevMode()).toBe(false)
    setDevMode(before)
  })
})
```

- [ ] **Step 2：跑测试确认失败**

Run: `npx vitest run packages/engine/tests/invariants.spec.ts`
Expected: 2 failed

- [ ] **Step 3：实现 `packages/engine/src/invariants.ts`（仅框架部分）**

```ts
import type { EngineState, Invariant } from './types'

let DEV_MODE = false

export function setDevMode(on: boolean): void {
  DEV_MODE = on
}

export function isDevMode(): boolean {
  return DEV_MODE
}

/**
 * 检查所有不变量；返回违反列表。
 * 调用方决定是否抛错（dev throw / prod emit event）。
 */
export function assertInvariants(state: EngineState): Invariant[] {
  const violations: Invariant[] = []
  for (const check of CHECKS) check(state, violations)
  return violations
}

type Check = (state: EngineState, out: Invariant[]) => void
const CHECKS: Check[] = []

export function _registerCheck(check: Check): void {
  CHECKS.push(check)
}
```

- [ ] **Step 4：跑测试确认通过**

Run: `npx vitest run packages/engine/tests/invariants.spec.ts`
Expected: 2 passed

- [ ] **Step 5：在 step.ts 接入 assertInvariants**

把 `step()` 函数的 `const violations: Invariant[] = []` 替换为：

```ts
import { assertInvariants, isDevMode } from './invariants'

// ... 在 commitMoves 之后：
const violations = assertInvariants(next)
if (violations.length > 0) {
  for (const v of violations) {
    events.push({ tick: next.tick, kind: 'invariant_violation', rule: v.rule, detail: v.detail })
  }
  if (isDevMode()) {
    throw new Error(
      `Invariant violations at tick ${next.tick}: ${violations.map((v) => v.rule).join(', ')}`,
    )
  }
}
```

- [ ] **Step 6：在 `packages/engine/src/index.ts` 导出**

追加：

```ts
export { assertInvariants, isDevMode, setDevMode } from './invariants'
```

- [ ] **Step 7：跑所有测试确认未破坏**

Run: `npx vitest run packages/engine/`
Expected: 全 pass

- [ ] **Step 8：commit**

```bash
git add packages/engine/src/invariants.ts packages/engine/src/step.ts packages/engine/src/index.ts packages/engine/tests/invariants.spec.ts
git commit -m "feat(engine): 添加 assertInvariants 框架与 dev/prod 模式切换"
```

---

### Task 16–22：7 条不变量（每条一个 task）

每条不变量结构相同：写违法状态测试 → 验证抓到 → 实现检查 → 验证抓到 → commit。下面 7 条全部展开。

#### Task 16：不变量 R1 — 列车状态与速度一致性

> `state='moving'` 但 `speed=0` 持续超过 N tick → 违反（典型"原地不动"症状之一）

引擎本任务**只能在每 tick 累加 staleStoppedTicks** + 在不变量检查时读取阈值。这要求：
- step.ts 在 commit 后扫描 trains 并更新 `staleStoppedTicks`
- invariants 检查阈值

阈值 = 60 tick（1 秒）

**Files:**
- Modify: `packages/engine/src/step.ts`
- Modify: `packages/engine/src/invariants.ts`
- Modify: `packages/engine/tests/invariants.spec.ts`

- [ ] **Step 1：在 `invariants.spec.ts` 追加测试**

```ts
import type { EngineState, TrainPhysics } from '@engine'
import { _registerCheck } from '@engine/invariants'
import { invariantR1MovingButZeroSpeed } from '@engine/invariants'  // 直接 export 用以单测

function stateWith(train: Partial<TrainPhysics>): EngineState {
  return {
    tick: 0, trains: [{
      id: 't', modelType: 'CR400AF', isCoupled: false, currentEdgeId: 'e',
      position: 0, speed: 0, state: 'stopped', direction: 1,
      path: [], visitedPath: [], ...train,
    }],
    rng: createPRNG(1),
    railMap: { nodes: {}, edges: {}, platforms: [] },
  }
}

describe('R1 — moving 但 speed=0', () => {
  it('staleStoppedTicks ≤ 60 不报', () => {
    const s = stateWith({ state: 'moving', speed: 0, staleStoppedTicks: 60 })
    const out: Invariant[] = []
    invariantR1MovingButZeroSpeed(s, out)
    expect(out).toHaveLength(0)
  })
  it('staleStoppedTicks > 60 报', () => {
    const s = stateWith({ state: 'moving', speed: 0, staleStoppedTicks: 61 })
    const out: Invariant[] = []
    invariantR1MovingButZeroSpeed(s, out)
    expect(out[0]?.rule).toBe('R1_moving_zero_speed')
  })
  it('正常 moving + speed>0 不报', () => {
    const s = stateWith({ state: 'moving', speed: 60, staleStoppedTicks: 0 })
    const out: Invariant[] = []
    invariantR1MovingButZeroSpeed(s, out)
    expect(out).toHaveLength(0)
  })
})
```

- [ ] **Step 2：在 `invariants.ts` 追加实现 + 自动注册**

```ts
import type { Invariant } from './types'

const R1_THRESHOLD_TICKS = 60

export function invariantR1MovingButZeroSpeed(
  state: { trains: { id: string; state: 'moving' | 'stopped'; speed: number; staleStoppedTicks?: number }[] },
  out: Invariant[],
): void {
  for (const t of state.trains) {
    if (t.state === 'moving' && t.speed === 0 && (t.staleStoppedTicks ?? 0) > R1_THRESHOLD_TICKS) {
      out.push({
        rule: 'R1_moving_zero_speed',
        detail: { trainId: t.id, ticks: t.staleStoppedTicks },
      })
    }
  }
}

_registerCheck(invariantR1MovingButZeroSpeed)
```

- [ ] **Step 3：在 step.ts 维护 staleStoppedTicks**

在 commitMoves 之后追加：

```ts
for (const t of next.trains) {
  if (t.speed === 0) {
    t.staleStoppedTicks = (t.staleStoppedTicks ?? 0) + 1
  } else {
    t.staleStoppedTicks = 0
  }
}
```

- [ ] **Step 4：跑测试确认通过**

Run: `npx vitest run packages/engine/tests/invariants.spec.ts`
Expected: 上 3 条 pass

- [ ] **Step 5：commit**

```bash
git add packages/engine/src/invariants.ts packages/engine/src/step.ts packages/engine/tests/invariants.spec.ts
git commit -m "feat(engine): 不变量 R1 — moving 但持续 speed=0"
```

---

#### Task 17：不变量 R2 — 卡死的停车列车（应该恢复但没动）

> `state='stopped'` + `boardingTimer=0` + `passengerState!=='BOARDING'` + 下条边信号绿 → 应被 tryResume；否则连续 N tick 仍卡 → 违反

阈值 = 120 tick（2 秒）。

为简化检查，这条规则需要邻接表。我们在 invariants 之外构造一次邻接表传入。

**Files:**
- Modify: `packages/engine/src/invariants.ts`
- Modify: `packages/engine/src/step.ts`
- Modify: `packages/engine/tests/invariants.spec.ts`

- [ ] **Step 1：扩展 invariants 框架，使 check 可拿到 adj**

修改 `invariants.ts`：

```ts
import type { AdjacencyIndex } from './adjacency'

type Ctx = { state: EngineState; adj: AdjacencyIndex }
type Check = (ctx: Ctx, out: Invariant[]) => void
const CHECKS: Check[] = []
export function _registerCheck(check: Check): void { CHECKS.push(check) }

export function assertInvariants(state: EngineState, adj: AdjacencyIndex): Invariant[] {
  const violations: Invariant[] = []
  const ctx = { state, adj }
  for (const check of CHECKS) check(ctx, violations)
  return violations
}
```

修改 step.ts 调用：

```ts
const violations = assertInvariants(next, adj)
```

R1 的实现也要改签名（取 `ctx.state`）。

- [ ] **Step 2：在 step.ts 维护 trains 上的 stuckResumableTicks（额外字段）**

在 types.ts `TrainPhysics` 加 `stuckResumableTicks?: number`，step.ts commitMoves 后：

```ts
for (const t of next.trains) {
  const stopped = t.state === 'stopped'
  const noPassenger = t.passengerState === undefined
  if (stopped && noPassenger) {
    t.stuckResumableTicks = (t.stuckResumableTicks ?? 0) + 1
  } else {
    t.stuckResumableTicks = 0
  }
}
```

- [ ] **Step 3：写 R2 测试**

```ts
describe('R2 — stuck stopped 应能恢复但没动', () => {
  it('stuckResumableTicks > 120 + 下条边信号绿 → 报', () => {
    const map = makeMap()  // 复用 intent.spec 中的工厂；导入或就地定义
    const t: TrainPhysics = {
      id: 't', modelType: 'CR400AF', isCoupled: false,
      currentEdgeId: 'e_ab', position: 100, speed: 0, state: 'stopped',
      direction: 1, path: [], visitedPath: [],
      stuckResumableTicks: 121,
    }
    const adj = buildAdjacency(map)
    const out: Invariant[] = []
    invariantR2StuckResumable({ state: { trains: [t] } as any, adj }, out)
    expect(out[0]?.rule).toBe('R2_stuck_resumable')
  })
})
```

- [ ] **Step 4：实现 R2**

```ts
import { resolveNextEdge } from './adjacency'

const R2_THRESHOLD_TICKS = 120

export function invariantR2StuckResumable(ctx: Ctx, out: Invariant[]): void {
  for (const t of ctx.state.trains) {
    if (t.state !== 'stopped') continue
    if ((t.stuckResumableTicks ?? 0) <= R2_THRESHOLD_TICKS) continue
    const edge = ctx.state.railMap.edges[t.currentEdgeId]
    if (!edge) continue
    const dir = t.direction
    const distToBound = dir === 1 ? edge.length - t.position : t.position
    if (distToBound > 0.1) continue
    const nextNodeId = dir === 1 ? edge.toNode : edge.fromNode
    const nextNode = ctx.state.railMap.nodes[nextNodeId]
    if (!nextNode) continue
    if (nextNode.signalState === 'red') continue
    const nextEdgeId = resolveNextEdge(nextNode, t.currentEdgeId, ctx.adj)
    if (!nextEdgeId) continue
    out.push({
      rule: 'R2_stuck_resumable',
      detail: { trainId: t.id, ticks: t.stuckResumableTicks, edgeId: t.currentEdgeId },
    })
  }
}

_registerCheck(invariantR2StuckResumable)
```

- [ ] **Step 5：测试通过 + commit**

```bash
git add packages/engine/src/invariants.ts packages/engine/src/step.ts packages/engine/src/types.ts packages/engine/tests/invariants.spec.ts
git commit -m "feat(engine): 不变量 R2 — 列车应能恢复但持续未动"
```

---

#### Task 18：不变量 R3 — 出站后未消失

> `isHandedOver=true` 持续 > 60s（3600 tick） 仍在 trains 数组 → 违反

**Files:**
- Modify: `packages/engine/src/invariants.ts`
- Modify: `packages/engine/src/step.ts`
- Modify: `packages/engine/tests/invariants.spec.ts`

- [ ] **Step 1：在 step.ts 维护 postHandoverTicks**

```ts
for (const t of next.trains) {
  if (t.isHandedOver) {
    t.postHandoverTicks = (t.postHandoverTicks ?? 0) + 1
  } else {
    t.postHandoverTicks = 0
  }
}
```

- [ ] **Step 2：写测试**

```ts
describe('R3 — handover 后未移除', () => {
  it('postHandoverTicks > 3600 → 报', () => {
    const t: TrainPhysics = {
      id: 't', modelType: 'CR400AF', isCoupled: false, currentEdgeId: 'e',
      position: 0, speed: 0, state: 'stopped', direction: 1,
      path: [], visitedPath: [], isHandedOver: true, postHandoverTicks: 3601,
    }
    const out: Invariant[] = []
    invariantR3HandoverNotRemoved({ state: { trains: [t] } as any, adj: new Map() }, out)
    expect(out[0]?.rule).toBe('R3_handover_not_removed')
  })
})
```

- [ ] **Step 3：实现**

```ts
const R3_THRESHOLD_TICKS = 3600

export function invariantR3HandoverNotRemoved(ctx: Ctx, out: Invariant[]): void {
  for (const t of ctx.state.trains) {
    if (t.isHandedOver && (t.postHandoverTicks ?? 0) > R3_THRESHOLD_TICKS) {
      out.push({
        rule: 'R3_handover_not_removed',
        detail: { trainId: t.id, ticks: t.postHandoverTicks },
      })
    }
  }
}

_registerCheck(invariantR3HandoverNotRemoved)
```

- [ ] **Step 4：测试通过 + commit**

```bash
git add packages/engine/src/invariants.ts packages/engine/src/step.ts packages/engine/tests/invariants.spec.ts
git commit -m "feat(engine): 不变量 R3 — handover 后超时未移除"
```

---

#### Task 19：不变量 R4 — path 空但 state≠stopped

- [ ] **Step 1：测试**

```ts
describe('R4 — path 空但 state≠stopped', () => {
  it('path=[] + state=moving → 报', () => {
    const t: TrainPhysics = {
      id: 't', modelType: 'CR400AF', isCoupled: false, currentEdgeId: 'e',
      position: 0, speed: 0, state: 'moving', direction: 1,
      path: [], visitedPath: [],
    }
    const out: Invariant[] = []
    invariantR4EmptyPathMoving({ state: { trains: [t] } as any, adj: new Map() }, out)
    expect(out[0]?.rule).toBe('R4_empty_path_moving')
  })
  it('path=[] + state=stopped → 不报', () => {
    const t: TrainPhysics = {
      id: 't', modelType: 'CR400AF', isCoupled: false, currentEdgeId: 'e',
      position: 0, speed: 0, state: 'stopped', direction: 1,
      path: [], visitedPath: [],
    }
    const out: Invariant[] = []
    invariantR4EmptyPathMoving({ state: { trains: [t] } as any, adj: new Map() }, out)
    expect(out).toHaveLength(0)
  })
})
```

- [ ] **Step 2：实现**

```ts
export function invariantR4EmptyPathMoving(ctx: Ctx, out: Invariant[]): void {
  for (const t of ctx.state.trains) {
    if ((!t.path || t.path.length === 0) && t.state === 'moving') {
      out.push({
        rule: 'R4_empty_path_moving',
        detail: { trainId: t.id, edgeId: t.currentEdgeId },
      })
    }
  }
}

_registerCheck(invariantR4EmptyPathMoving)
```

注意：列车在已经停在终端边等待出站时也可能 `path=[]` 但 state=stopped，这是正常状态。R4 只抓 `moving` 情况。

- [ ] **Step 3：测试通过 + commit**

```bash
git add packages/engine/src/invariants.ts packages/engine/tests/invariants.spec.ts
git commit -m "feat(engine): 不变量 R4 — path 空但仍 moving"
```

---

#### Task 20：不变量 R5 — 边占用与列车 currentEdgeId 一致

- [ ] **Step 1：测试**

```ts
describe('R5 — occupiedBy 与 currentEdgeId 一致', () => {
  it('列车在 e1，但 e1.occupiedBy 为另一列 → 报', () => {
    const map: RailMap = {
      nodes: {},
      edges: {
        e1: { id: 'e1', fromNode: 'a', toNode: 'b', length: 100, occupiedBy: 'OTHER' },
      },
      platforms: [],
    }
    const t: TrainPhysics = {
      id: 't', modelType: 'CR400AF', isCoupled: false, currentEdgeId: 'e1',
      position: 50, speed: 0, state: 'stopped', direction: 1,
      path: [], visitedPath: [],
    }
    const out: Invariant[] = []
    invariantR5OccupancyMismatch({ state: { trains: [t], railMap: map } as any, adj: new Map() }, out)
    expect(out[0]?.rule).toBe('R5_occupancy_mismatch')
  })
})
```

- [ ] **Step 2：实现**

```ts
export function invariantR5OccupancyMismatch(ctx: Ctx, out: Invariant[]): void {
  for (const t of ctx.state.trains) {
    if (t.isHandedOver) continue
    const edge = ctx.state.railMap.edges[t.currentEdgeId]
    if (!edge) continue
    if (edge.occupiedBy !== null && edge.occupiedBy !== t.id) {
      out.push({
        rule: 'R5_occupancy_mismatch',
        detail: { trainId: t.id, edgeId: t.currentEdgeId, occupiedBy: edge.occupiedBy },
      })
    }
  }
}
_registerCheck(invariantR5OccupancyMismatch)
```

- [ ] **Step 3：测试通过 + commit**

```bash
git add packages/engine/src/invariants.ts packages/engine/tests/invariants.spec.ts
git commit -m "feat(engine): 不变量 R5 — 边占用与列车归属不一致"
```

---

#### Task 21：不变量 R6 — direction 取值合法

- [ ] **Step 1：测试**

```ts
describe('R6 — direction ∈ {-1, 1}', () => {
  it('direction=0 → 报', () => {
    const t: TrainPhysics = {
      id: 't', modelType: 'CR400AF', isCoupled: false, currentEdgeId: 'e',
      position: 0, speed: 0, state: 'stopped',
      direction: 0 as 1 | -1, path: [], visitedPath: [],
    }
    const out: Invariant[] = []
    invariantR6BadDirection({ state: { trains: [t] } as any, adj: new Map() }, out)
    expect(out[0]?.rule).toBe('R6_bad_direction')
  })
})
```

- [ ] **Step 2：实现**

```ts
export function invariantR6BadDirection(ctx: Ctx, out: Invariant[]): void {
  for (const t of ctx.state.trains) {
    if (t.direction !== 1 && t.direction !== -1) {
      out.push({
        rule: 'R6_bad_direction',
        detail: { trainId: t.id, direction: t.direction },
      })
    }
  }
}
_registerCheck(invariantR6BadDirection)
```

- [ ] **Step 3：commit**

```bash
git add packages/engine/src/invariants.ts packages/engine/tests/invariants.spec.ts
git commit -m "feat(engine): 不变量 R6 — direction 取值非法"
```

---

#### Task 22：不变量 R7 — currentEdgeId 必须存在于 railMap

- [ ] **Step 1：测试**

```ts
describe('R7 — currentEdgeId 存在', () => {
  it('列车在不存在的边 → 报', () => {
    const map: RailMap = { nodes: {}, edges: {}, platforms: [] }
    const t: TrainPhysics = {
      id: 't', modelType: 'CR400AF', isCoupled: false, currentEdgeId: 'no_such',
      position: 0, speed: 0, state: 'stopped', direction: 1,
      path: [], visitedPath: [],
    }
    const out: Invariant[] = []
    invariantR7MissingEdge({ state: { trains: [t], railMap: map } as any, adj: new Map() }, out)
    expect(out[0]?.rule).toBe('R7_missing_edge')
  })
})
```

- [ ] **Step 2：实现**

```ts
export function invariantR7MissingEdge(ctx: Ctx, out: Invariant[]): void {
  for (const t of ctx.state.trains) {
    if (!ctx.state.railMap.edges[t.currentEdgeId]) {
      out.push({
        rule: 'R7_missing_edge',
        detail: { trainId: t.id, edgeId: t.currentEdgeId },
      })
    }
  }
}
_registerCheck(invariantR7MissingEdge)
```

- [ ] **Step 3：跑全部不变量测试 + commit**

Run: `npx vitest run packages/engine/tests/invariants.spec.ts`
Expected: 全 pass

```bash
git add packages/engine/src/invariants.ts packages/engine/tests/invariants.spec.ts
git commit -m "feat(engine): 不变量 R7 — currentEdgeId 必须存在"
```

至此 7 条不变量 + 框架 = 8 条规则到位。

---

## Phase G：事件流接入

### Task 23：EventBus + 事件流导出

引擎已经在 step() 返回 events 数组；现在需要 UI 层有一个 EventBuffer 累积，并提供"导出 JSON"按钮。

**Files:**
- Create: `src/game/EventBus.ts`
- Create: `src/game/__tests__/EventBus.spec.ts`
- Modify: `src/App.vue`

- [ ] **Step 1：写测试 `src/game/__tests__/EventBus.spec.ts`**

```ts
import { describe, expect, it } from 'vitest'
import { EventBus } from '@/game/EventBus'
import type { EngineEvent } from '@engine'

function ev(tick: number): EngineEvent {
  return { tick, kind: 'spawn', trainId: 't', modelType: 'CR400AF', entrance: 'L' }
}

describe('EventBus', () => {
  it('emit 把事件推入缓冲且通知订阅者', () => {
    const bus = new EventBus(100)
    const seen: EngineEvent[] = []
    const off = bus.subscribe((e) => seen.push(e))
    bus.emit([ev(1), ev(2)])
    expect(seen).toHaveLength(2)
    expect(bus.snapshot()).toHaveLength(2)
    off()
    bus.emit([ev(3)])
    expect(seen).toHaveLength(2) // 取消订阅后不再回调
  })

  it('exportJSON 返回带元数据的 JSON 字符串', () => {
    const bus = new EventBus(10)
    bus.emit([ev(1)])
    const json = bus.exportJSON({ seed: 42, engineVersion: '0.1.0' })
    const obj = JSON.parse(json)
    expect(obj.seed).toBe(42)
    expect(obj.engineVersion).toBe('0.1.0')
    expect(obj.events).toHaveLength(1)
  })

  it('clear 清空但保留订阅者', () => {
    const bus = new EventBus(10)
    bus.emit([ev(1)])
    bus.clear()
    expect(bus.snapshot()).toHaveLength(0)
    let count = 0
    bus.subscribe(() => count++)
    bus.emit([ev(2)])
    expect(count).toBe(1)
  })
})
```

- [ ] **Step 2：跑测试确认失败**

Run: `npx vitest run src/game/__tests__/EventBus.spec.ts`
Expected: 3 failed

- [ ] **Step 3：实现 `src/game/EventBus.ts`**

```ts
import type { EngineEvent } from '@engine'
import { EventBuffer } from '@engine'

export type EventListener = (event: EngineEvent) => void

export interface ExportMeta {
  seed: number
  engineVersion: string
  stationId?: string
  stationVersion?: string
  startedAt?: string
}

/**
 * UI 层事件总线：累积引擎事件、广播给订阅者、提供导出。
 * 不与引擎内部状态耦合；引擎用 EventBuffer，这里也用一份独立 EventBuffer。
 */
export class EventBus {
  private buffer: EventBuffer
  private listeners: Set<EventListener> = new Set()

  constructor(capacity: number) {
    this.buffer = new EventBuffer(capacity)
  }

  emit(events: EngineEvent[]): void {
    for (const e of events) {
      this.buffer.push(e)
      for (const l of this.listeners) l(e)
    }
  }

  subscribe(listener: EventListener): () => void {
    this.listeners.add(listener)
    return () => this.listeners.delete(listener)
  }

  snapshot(): EngineEvent[] {
    return this.buffer.snapshot()
  }

  clear(): void {
    this.buffer.clear()
  }

  exportJSON(meta: ExportMeta): string {
    return JSON.stringify({ ...meta, events: this.buffer.snapshot() }, null, 2)
  }
}
```

- [ ] **Step 4：跑测试确认通过**

Run: `npx vitest run src/game/__tests__/EventBus.spec.ts`
Expected: 3 passed

- [ ] **Step 5：在 `App.vue` 中实例化 + 接管事件**

```ts
import { EventBus } from '@/game/EventBus'

const eventBus = new EventBus(50_000)

// loop() 中：
const result = step(engineState.value, inputs)
engineState.value = result.next
eventBus.emit(result.events)
```

- [ ] **Step 6：在 RightPanel 加一个"导出事件流"按钮**

修改 `src/components/panels/RightPanel.vue` 加：

```html
<button class="btn-export" @click="$emit('exportEvents')">导出事件流</button>
```

App.vue 监听该 emit：

```ts
function handleExportEvents() {
  const json = eventBus.exportJSON({
    seed: engineSeed.value,
    engineVersion: '0.1.0',
    stationId: activeStation.value?.id,
  })
  const blob = new Blob([json], { type: 'application/json' })
  const url = URL.createObjectURL(blob)
  const a = document.createElement('a')
  a.href = url
  a.download = `events-${Date.now()}.json`
  a.click()
  URL.revokeObjectURL(url)
}
```

`engineSeed` 是新增的 `ref(seedFromHandleStationSelect)`。

- [ ] **Step 7：手动冒烟：玩 30 秒后导出 JSON 检查内容合理**

Run: `npm run dev`，玩一会儿，按导出，打开 JSON 文件确认包含 `seed/events`。

- [ ] **Step 8：commit**

```bash
git add src/game/EventBus.ts src/game/__tests__/EventBus.spec.ts src/App.vue src/components/panels/RightPanel.vue
git commit -m "feat: EventBus 接入引擎事件流并提供导出按钮"
```

---

## Phase H：App.vue 拆解为 6 模块

App.vue 的职责必须严格拆开。每个任务一个模块，先抽出最独立的，再抽逐步耦合的。

### Task 24：InputRecorder

**职责**：累积每个 tick 提交给 step() 的 EngineInput；提供 take()、snapshot()、reset()。

**Files:**
- Create: `src/game/InputRecorder.ts`
- Create: `src/game/__tests__/InputRecorder.spec.ts`
- Modify: `src/App.vue`

- [ ] **Step 1：测试**

```ts
import { describe, expect, it } from 'vitest'
import { InputRecorder } from '@/game/InputRecorder'

describe('InputRecorder', () => {
  it('record + take 清空缓冲', () => {
    const r = new InputRecorder()
    r.record({ tick: 0, type: 'stop', payload: { trainId: 't' } })
    r.record({ tick: 0, type: 'stop', payload: { trainId: 'u' } })
    expect(r.take()).toHaveLength(2)
    expect(r.take()).toHaveLength(0)
  })

  it('snapshot 返回完整流，不影响 take()', () => {
    const r = new InputRecorder()
    r.record({ tick: 0, type: 'stop', payload: { trainId: 't' } })
    expect(r.snapshot()).toHaveLength(1)
    r.take()
    // 全流仍保留
    expect(r.snapshot()).toHaveLength(1)
  })

  it('reset 清空全流', () => {
    const r = new InputRecorder()
    r.record({ tick: 0, type: 'stop', payload: { trainId: 't' } })
    r.reset()
    expect(r.snapshot()).toHaveLength(0)
  })
})
```

- [ ] **Step 2：实现**

```ts
import type { EngineInput } from '@engine'

export class InputRecorder {
  private pending: EngineInput[] = []
  private full: EngineInput[] = []

  record(input: EngineInput): void {
    this.pending.push(input)
    this.full.push(input)
  }

  /** 取出本 tick 待消费输入，并清空 pending（保留 full）。 */
  take(): EngineInput[] {
    const r = this.pending
    this.pending = []
    return r
  }

  /** 自开局至今全部输入（用于 replay 上传）。 */
  snapshot(): EngineInput[] {
    return [...this.full]
  }

  reset(): void {
    this.pending = []
    this.full = []
  }
}
```

- [ ] **Step 3：替换 App.vue 的 `pendingInputs.value`，改用 InputRecorder**

```ts
import { InputRecorder } from '@/game/InputRecorder'

const inputRecorder = new InputRecorder()
// 原 pendingInputs.value.push(...) → inputRecorder.record(...)
// loop() 中 step(state, inputRecorder.take())
```

- [ ] **Step 4：跑测试 + commit**

```bash
git add src/game/InputRecorder.ts src/game/__tests__/InputRecorder.spec.ts src/App.vue
git commit -m "refactor: 抽取 InputRecorder 模块"
```

---

### Task 25：GameLoop

**职责**：requestAnimationFrame、固定时间步累加器、倍速、暂停。

**Files:**
- Create: `src/game/GameLoop.ts`
- Create: `src/game/__tests__/GameLoop.spec.ts`
- Modify: `src/App.vue`

- [ ] **Step 1：测试**

```ts
import { describe, expect, it } from 'vitest'
import { GameLoop } from '@/game/GameLoop'

describe('GameLoop', () => {
  it('暂停期间 onTick 不调用', () => {
    let calls = 0
    const loop = new GameLoop({ fixedStep: 1 / 60, onTick: () => calls++ })
    loop.setSpeed(0)
    loop.advance(1000)  // 1000 秒：speed=0 故不推进
    expect(calls).toBe(0)
  })

  it('1x 倍速 1 秒后 onTick 调用约 60 次', () => {
    let calls = 0
    const loop = new GameLoop({ fixedStep: 1 / 60, onTick: () => calls++ })
    loop.setSpeed(1)
    loop.advance(1)  // 1 秒（advance 接受秒数）
    expect(calls).toBe(60)
  })

  it('5x 倍速 1 秒后 onTick 调用约 300 次', () => {
    let calls = 0
    const loop = new GameLoop({ fixedStep: 1 / 60, onTick: () => calls++ })
    loop.setSpeed(5)
    loop.advance(1)
    expect(calls).toBe(300)
  })

  it('累加器不超出（防死亡螺旋，单次 advance 上限）', () => {
    let calls = 0
    const loop = new GameLoop({ fixedStep: 1 / 60, onTick: () => calls++, maxTicksPerAdvance: 100 })
    loop.setSpeed(1)
    loop.advance(10)  // 10 秒：本应 600 tick，但被 cap 到 100
    expect(calls).toBe(100)
  })
})
```

- [ ] **Step 2：实现**

```ts
export interface GameLoopOptions {
  fixedStep: number       // 单 tick 时间，秒
  onTick: () => void
  maxTicksPerAdvance?: number  // 默认 600（10 秒），防死亡螺旋
}

export class GameLoop {
  private accumulator = 0
  private speed = 1
  private rafId: number | null = null
  private lastTime = 0
  private readonly fixedStep: number
  private readonly onTick: () => void
  private readonly cap: number

  constructor(opts: GameLoopOptions) {
    this.fixedStep = opts.fixedStep
    this.onTick = opts.onTick
    this.cap = opts.maxTicksPerAdvance ?? 600
  }

  setSpeed(s: number): void {
    this.speed = s
  }

  /** 推进 dtSeconds 秒（用于测试或单步 step）。 */
  advance(dtSeconds: number): void {
    if (this.speed === 0) return
    this.accumulator += dtSeconds * this.speed
    let ticks = 0
    while (this.accumulator >= this.fixedStep && ticks < this.cap) {
      this.onTick()
      this.accumulator -= this.fixedStep
      ticks++
    }
    if (ticks >= this.cap) this.accumulator = 0
  }

  start(): void {
    if (this.rafId !== null) return
    this.lastTime = performance.now()
    const tick = (t: number) => {
      const dt = (t - this.lastTime) / 1000
      this.lastTime = t
      this.advance(dt)
      this.rafId = requestAnimationFrame(tick)
    }
    this.rafId = requestAnimationFrame(tick)
  }

  stop(): void {
    if (this.rafId !== null) {
      cancelAnimationFrame(this.rafId)
      this.rafId = null
    }
  }
}
```

- [ ] **Step 3：在 App.vue 替换原 loop()/requestAnimationFrame 逻辑**

```ts
import { GameLoop } from '@/game/GameLoop'

const gameLoop = new GameLoop({
  fixedStep: 1 / 60,
  onTick: () => {
    const result = step(engineState.value, inputRecorder.take())
    engineState.value = result.next
    eventBus.emit(result.events)
  },
})

onMounted(() => gameLoop.start())
onUnmounted(() => gameLoop.stop())

watch(gameSpeed, (s) => gameLoop.setSpeed(s))
```

- [ ] **Step 4：跑测试 + commit**

```bash
git add src/game/GameLoop.ts src/game/__tests__/GameLoop.spec.ts src/App.vue
git commit -m "refactor: 抽取 GameLoop 模块（固定步长 + 倍速 + 暂停）"
```

---

### Task 26：TrainSpawner

**职责**：监听 ScheduleManager 的 waiting 队列；接到 ADMIT 时，根据车站拓扑生成 `TrainSpawnSpec` 并 record 一条 admit input。

**Files:**
- Create: `src/game/TrainSpawner.ts`
- Create: `src/game/__tests__/TrainSpawner.spec.ts`
- Modify: `src/App.vue`

- [ ] **Step 1：测试**

```ts
import { describe, expect, it } from 'vitest'
import { TrainSpawner } from '@/game/TrainSpawner'
import type { ScheduleEntry } from '@/core/types'

describe('TrainSpawner', () => {
  it('小型车站：生成上行列车的入口与 path', () => {
    const sp = new TrainSpawner({ stationType: 'small' })
    const entry: ScheduleEntry = {
      id: 'G2', direction: 'up', model: 'CR400AF',
      scheduledArriveTick: 100, scheduledStopDuration: 1800,
      scheduledDepartTick: 1900, currentDelay: 0, status: 'waiting',
    }
    const spec = sp.buildSpec(entry, /*platformNum*/ 2, /*isCoupled*/ false)
    expect(spec.currentEdgeId).toBe('e_entry_L')
    expect(spec.path).toEqual(['e_L_t2', 't2'])
    expect(spec.scheduleEntryId).toBe('G2')
  })

  it('终端车站：上行从 e_entry_U', () => {
    const sp = new TrainSpawner({ stationType: 'terminal' })
    const entry: ScheduleEntry = {
      id: 'G2', direction: 'up', model: 'CR400AF',
      scheduledArriveTick: 100, scheduledStopDuration: 1800,
      scheduledDepartTick: 1900, currentDelay: 0, status: 'waiting',
    }
    const spec = sp.buildSpec(entry, 1, false)
    expect(spec.currentEdgeId).toBe('e_entry_U')
  })

  it('终端车站：下行从 e_entry_D', () => {
    const sp = new TrainSpawner({ stationType: 'terminal' })
    const entry: ScheduleEntry = {
      id: 'G3', direction: 'down', model: 'CR400AF',
      scheduledArriveTick: 100, scheduledStopDuration: 1800,
      scheduledDepartTick: 1900, currentDelay: 0, status: 'waiting',
    }
    const spec = sp.buildSpec(entry, 1, false)
    expect(spec.currentEdgeId).toBe('e_entry_D')
  })
})
```

- [ ] **Step 2：实现**

```ts
import type { ScheduleEntry } from '@/core/types'
import type { TrainSpawnSpec } from '@engine'

export interface SpawnerOptions {
  stationType: 'small' | 'terminal' | 'hub'
}

export class TrainSpawner {
  constructor(private opts: SpawnerOptions) {}

  buildSpec(entry: ScheduleEntry, platformNum: number, isCoupled: boolean): TrainSpawnSpec {
    let currentEdgeId: string
    let path: string[]
    if (this.opts.stationType === 'terminal') {
      currentEdgeId = entry.direction === 'up' ? 'e_entry_U' : 'e_entry_D'
      path = [`t${platformNum}`]
    } else {
      // small / hub 复用入口
      currentEdgeId = 'e_entry_L'
      path = [`e_L_t${platformNum}`, `t${platformNum}`]
    }
    return {
      id: entry.id,
      modelType: entry.model,
      isCoupled,
      currentEdgeId,
      path,
      direction: 1,
      speed: 60,
      scheduleEntryId: entry.id,
      scheduledArriveTick: entry.scheduledArriveTick,
    }
  }
}
```

- [ ] **Step 3：在 App.vue 用 TrainSpawner 重写 spawnTrainIntoMap**

```ts
import { TrainSpawner } from '@/game/TrainSpawner'
import { randomChance, randomInt } from '@engine'

const spawner = ref<TrainSpawner | null>(null)
// handleStationSelect 中:
spawner.value = new TrainSpawner({ stationType: config.type as 'small' | 'terminal' })

function spawnTrainIntoMap(id: string): boolean {
  const entry = waitingQueue.value.find((q) => q.id === id)
  if (!entry || !spawner.value) return false
  const platformNum = randomInt(uiRng, 1, 4)
  const isCoupled = randomChance(uiRng, 0.2)
  const spec = spawner.value.buildSpec(entry, platformNum, isCoupled)
  // SPAWN SAFETY CHECK
  const blocked = engineState.value.trains.some(
    (t) => t.currentEdgeId === spec.currentEdgeId && t.position < 300,
  )
  if (blocked) {
    showToast('入口拥堵！前车尚未驶离安全区')
    return false
  }
  inputRecorder.record({
    tick: engineState.value.tick,
    type: 'admit',
    payload: { trainSpec: spec },
  })
  return true
}
```

- [ ] **Step 4：跑测试 + commit**

```bash
git add src/game/TrainSpawner.ts src/game/__tests__/TrainSpawner.spec.ts src/App.vue
git commit -m "refactor: 抽取 TrainSpawner 模块"
```

---

### Task 27：DispatchController

**职责**：实现 ADMIT/DEPART/STOP 业务规则。DEPART 的 4 种策略（已在出站轨/终端折返/标准出站/兜底）在这里。

**Files:**
- Create: `src/game/DispatchController.ts`
- Create: `src/game/__tests__/DispatchController.spec.ts`
- Modify: `src/App.vue`

- [ ] **Step 1：测试（核心 4 种策略各 1 例）**

```ts
import { describe, expect, it } from 'vitest'
import { DispatchController } from '@/game/DispatchController'
import type { EngineState, RailMap, TrainPhysics } from '@engine'
import { createPRNG } from '@engine'

function smallStateWithTrainOnPlatform(): EngineState {
  // 简化：仅放一个含 t1 站台 / e_exit 出口的小图
  const map: RailMap = {
    nodes: {
      n_exit: { id: 'n_exit', x: 0, y: 0, type: 'endpoint' },
      n_t1_end: { id: 'n_t1_end', x: 0, y: 0, type: 'connector' },
      n_out_start: { id: 'n_out_start', x: 0, y: 0, type: 'connector' },
    },
    edges: {
      t1: { id: 't1', fromNode: 'n_t1_end', toNode: 'n_out_start', length: 100, occupiedBy: null, isPlatform: true },
      t1_out: { id: 't1_out', fromNode: 'n_out_start', toNode: 'n_exit', length: 100, occupiedBy: null },
      e_exit: { id: 'e_exit', fromNode: 'n_exit', toNode: 'n_exit', length: 50, occupiedBy: null },
    },
    platforms: [],
  }
  const train: TrainPhysics = {
    id: 't', modelType: 'CR400AF', isCoupled: false,
    currentEdgeId: 't1', position: 100, speed: 0, state: 'stopped',
    direction: 1, path: [], visitedPath: [],
    passengerState: 'READY', boardingTimer: 0,
  }
  return { tick: 0, trains: [train], railMap: map, rng: createPRNG(1) }
}

describe('DispatchController.computeDepartPath', () => {
  it('标准出站：从站台 t1 经 t1_out 到 e_exit', () => {
    const ctrl = new DispatchController()
    const state = smallStateWithTrainOnPlatform()
    const r = ctrl.computeDepartPath(state, 't', 'n_exit')
    expect(r.strategy).toBe('standard')
    expect(r.path).toEqual(['t1_out', 'e_exit'])
  })
})
```

- [ ] **Step 2：实现（核心 BFS + 4 策略）**

```ts
import type { EngineState, RailMap } from '@engine'

export interface DepartPlan {
  strategy: 'direct' | 'reverse' | 'standard' | 'fallback'
  /** 列车出站后还要走的边序列（不包含当前 currentEdgeId）。 */
  path: string[]
}

export class DispatchController {
  computeDepartPath(state: EngineState, trainId: string, targetExitNodeId: string): DepartPlan {
    const train = state.trains.find((t) => t.id === trainId)
    if (!train) return { strategy: 'fallback', path: [] }

    const onExitTrack = train.currentEdgeId.endsWith('_out') || train.currentEdgeId === 'e_exit'
    if (onExitTrack) {
      return { strategy: 'direct', path: bfs(state.railMap, train.currentEdgeId, targetExitNodeId) }
    }

    // 终端折返：当前边有对应的 t{n}_rev 边
    const revId = `${train.currentEdgeId}_rev`
    if (state.railMap.edges[revId]) {
      return { strategy: 'reverse', path: [revId, ...bfs(state.railMap, revId, targetExitNodeId)] }
    }

    // 标准：相邻 _out 轨道
    const outId = `${train.currentEdgeId}_out`
    if (state.railMap.edges[outId]) {
      return { strategy: 'standard', path: [outId, ...bfs(state.railMap, outId, targetExitNodeId)] }
    }

    // 兜底
    return { strategy: 'fallback', path: bfs(state.railMap, train.currentEdgeId, targetExitNodeId) }
  }
}

/** 边粒度 BFS：从 startEdgeId 找到目的节点 targetNodeId 的边序列（不含 startEdge）。 */
function bfs(map: RailMap, startEdgeId: string, targetNodeId: string): string[] {
  const startEdge = map.edges[startEdgeId]
  if (!startEdge) return []
  const queue: { edgeId: string; path: string[] }[] = [{ edgeId: startEdgeId, path: [] }]
  const visited = new Set<string>([startEdgeId])
  while (queue.length > 0) {
    const { edgeId, path } = queue.shift()!
    const edge = map.edges[edgeId]
    if (!edge) continue
    if (edge.toNode === targetNodeId) return path
    for (const next of Object.values(map.edges)) {
      if (visited.has(next.id)) continue
      if (next.fromNode === edge.toNode || next.toNode === edge.toNode) {
        visited.add(next.id)
        queue.push({ edgeId: next.id, path: [...path, next.id] })
      }
    }
  }
  return []
}
```

- [ ] **Step 3：在 App.vue 用 DispatchController 替换 handleDepartAction**

把原 `handleDepartAction` 内 4 段策略代码删掉，改为：

```ts
import { DispatchController } from '@/game/DispatchController'
const dispatch = new DispatchController()

function handleDepartAction(train: TrainPhysics): void {
  const plan = dispatch.computeDepartPath(engineState.value, train.id, getExitNodeId())
  // path 写入 train（通过 step 内 admit/depart 的现有路径，此处特殊：DEPART 需要先把 path 设好再发 depart input）
  // 先把 path 直接挂到 engineState 中对应 train（这是 UI 端的允许操作，因 step 不消费 path 直到 commit phase）
  const t = engineState.value.trains.find((x) => x.id === train.id)
  if (t) t.path = plan.path
  inputRecorder.record({
    tick: engineState.value.tick,
    type: 'depart',
    payload: { trainId: train.id, targetExitNodeId: getExitNodeId() },
  })
}
```

- [ ] **Step 4：跑测试 + commit**

```bash
git add src/game/DispatchController.ts src/game/__tests__/DispatchController.spec.ts src/App.vue
git commit -m "refactor: 抽取 DispatchController（DEPART 4 策略 + BFS）"
```

---

### Task 28：（合并到 Task 31 一起做 ReplayPlayer，先跳）

ReplayPlayer 与 Replay 面板在 Phase I 一起做。这里先空过 Task 28 编号到 Task 29 接 App.vue 收尾。

### Task 29：App.vue 减肥到 < 200 行

**Files:**
- Modify: `src/App.vue`

- [ ] **Step 1：把 generateKeyMappings 抽到 `src/game/keyMappings.ts`**

```ts
// src/game/keyMappings.ts
import type { RailMap } from '@engine'
import type { KeyboardControlConfig } from '@/core/types'

const SWITCH_KEYS = ['1', '2', '3', '4', '5', '6', '7', '8', '9', '0']
const SIGNAL_KEYS = ['q', 'w', 'e', 'r', 't', 'y', 'u', 'i', 'o', 'p']

export function generateKeyMappings(railMap: RailMap): KeyboardControlConfig[] {
  // 复制原 App.vue:308-360 函数体，import 类型；返回值不变
  // ... （原函数体）
}
```

- [ ] **Step 2：把 toast 系统抽到 `src/game/useToast.ts`（composable）**

```ts
// src/game/useToast.ts
import { ref } from 'vue'
import type { ToastEntry, ToastKind } from '@/components/Toast.vue'

export function useToast() {
  const toasts = ref<ToastEntry[]>([])
  let id = 0
  function pushToast(msg: string, kind: ToastKind = 'action', duration = 2400) {
    const tid = ++id
    toasts.value.push({ id: tid, msg, kind })
    setTimeout(() => {
      toasts.value = toasts.value.filter((t) => t.id !== tid)
    }, duration)
  }
  return { toasts, pushToast }
}
```

- [ ] **Step 3：把键盘事件处理 `handleKeyPress` 抽到 `src/game/useKeyboardControl.ts`**

把原 657-773 函数体放进 composable，参数化依赖项（gameLoop、selectedTrainId、inputRecorder 等）。

- [ ] **Step 4：把出站清理 `processExitingTrains` 抽到 `src/game/ExitProcessor.ts`**

```ts
// src/game/ExitProcessor.ts
import type { EngineState } from '@engine'
import type { InputRecorder } from './InputRecorder'

export class ExitProcessor {
  constructor(private inputRecorder: InputRecorder) {}

  process(state: EngineState): void {
    // 复制原 App.vue:195-237 函数体
    // 把 trains.splice(...) 改为：先标 isHandedOver，引擎层后续要支持 remove input
  }
}
```

由于 step.ts 当前不处理 `remove`，实际"消失"由 UI 层在 ExitProcessor 中通过 splice `engineState.value.trains` 完成（这是 UI 层允许的轻微 mutate，因为 trains 数组本身在每次 step 后是新副本）。

- [ ] **Step 5：删除 App.vue 中已被抽走的代码、合并 import**

- [ ] **Step 6：检查 App.vue 行数**

Run: `wc -l src/App.vue`
Expected: < 200 行

- [ ] **Step 7：手动冒烟（玩 1 局确认全功能正常）**

Run: `npm run dev` → 玩到出站

- [ ] **Step 8：跑全部测试 + 类型检查 + commit**

Run: `npm run type-check`
Run: `npx vitest run`
Expected: 全 pass

```bash
git add src/App.vue src/game/
git commit -m "refactor: App.vue 拆解为 6 模块，缩减到 < 200 行"
```

---

## Phase I：Replay 工具

### Task 30：ReplayPlayer

**Files:**
- Create: `src/game/ReplayPlayer.ts`
- Create: `src/game/__tests__/ReplayPlayer.spec.ts`

- [ ] **Step 1：测试**

```ts
import { describe, expect, it } from 'vitest'
import { ReplayPlayer } from '@/game/ReplayPlayer'
import type { EngineEvent, EngineInput, EngineState, RailMap } from '@engine'
import { createPRNG } from '@engine'

function emptyMap(): RailMap {
  return {
    nodes: { a: { id: 'a', x: 0, y: 0, type: 'connector', signalState: 'green' } },
    edges: {},
    platforms: [],
  }
}

describe('ReplayPlayer', () => {
  it('给定 seed + 0 输入 + 100 tick → 与原引擎跑出相同事件', () => {
    const seed = 42
    const initialState: EngineState = {
      tick: 0, trains: [], railMap: emptyMap(), rng: createPRNG(seed),
    }
    const player = new ReplayPlayer({ initialState, inputs: [], expectedEvents: [] })
    const result = player.run(100)
    expect(result.divergedAt).toBeNull()
    expect(result.finalTick).toBe(100)
  })

  it('期望事件流不匹配 → 返回 divergedAt = 第一处分叉 tick', () => {
    const initialState: EngineState = {
      tick: 0, trains: [], railMap: emptyMap(), rng: createPRNG(1),
    }
    const fakeExpected: EngineEvent[] = [
      { tick: 5, kind: 'collision', trainA: 'X', trainB: 'Y', edgeId: 'e' },
    ]
    const player = new ReplayPlayer({ initialState, inputs: [], expectedEvents: fakeExpected })
    const r = player.run(10)
    expect(r.divergedAt).toBe(5)
  })

  it('喂 signal_toggle 输入后下一 tick 信号变红', () => {
    const initialState: EngineState = {
      tick: 0, trains: [], railMap: emptyMap(), rng: createPRNG(1),
    }
    const inputs: EngineInput[] = [
      { tick: 0, type: 'signal_toggle', payload: { nodeId: 'a' } },
    ]
    const player = new ReplayPlayer({ initialState, inputs, expectedEvents: [] })
    player.run(1)
    expect(player.currentState.railMap.nodes.a!.signalState).toBe('red')
  })
})
```

- [ ] **Step 2：实现**

```ts
import type { EngineEvent, EngineInput, EngineState } from '@engine'
import { step } from '@engine'

export interface ReplayInit {
  initialState: EngineState
  inputs: EngineInput[]
  expectedEvents: EngineEvent[]
}

export interface ReplayResult {
  divergedAt: number | null      // 第一处事件不一致的 tick；null 表示一致
  finalTick: number
  events: EngineEvent[]
}

export class ReplayPlayer {
  private state: EngineState
  private readonly byTickInputs: Map<number, EngineInput[]>
  private readonly expected: EngineEvent[]
  private allEvents: EngineEvent[] = []

  constructor(init: ReplayInit) {
    this.state = JSON.parse(JSON.stringify(init.initialState))
    this.byTickInputs = new Map()
    for (const i of init.inputs) {
      const arr = this.byTickInputs.get(i.tick) ?? []
      arr.push(i)
      this.byTickInputs.set(i.tick, arr)
    }
    this.expected = init.expectedEvents
  }

  get currentState(): EngineState {
    return this.state
  }

  /** 推进 ticks 个 tick 或直到分叉。 */
  run(ticks: number): ReplayResult {
    let cmpIdx = 0
    for (let i = 0; i < ticks; i++) {
      const t = this.state.tick
      const inputs = this.byTickInputs.get(t) ?? []
      const r = step(this.state, inputs)
      this.state = r.next
      this.allEvents.push(...r.events)
      // 比对期望：取属于该 tick 的期望事件子集
      while (cmpIdx < this.expected.length && this.expected[cmpIdx]!.tick === t) {
        const exp = this.expected[cmpIdx]!
        const got = r.events.find(
          (e) => e.kind === exp.kind && e.tick === exp.tick &&
            JSON.stringify(e) === JSON.stringify(exp),
        )
        if (!got) {
          return {
            divergedAt: t,
            finalTick: this.state.tick,
            events: this.allEvents,
          }
        }
        cmpIdx++
      }
    }
    return { divergedAt: null, finalTick: this.state.tick, events: this.allEvents }
  }
}
```

- [ ] **Step 3：跑测试 + commit**

```bash
git add src/game/ReplayPlayer.ts src/game/__tests__/ReplayPlayer.spec.ts
git commit -m "feat: ReplayPlayer 用于回放与分叉点定位"
```

---

### Task 31：ReplayPanel.vue（dev-only）

**Files:**
- Create: `src/components/ReplayPanel.vue`
- Modify: `src/App.vue`

- [ ] **Step 1：实现 `src/components/ReplayPanel.vue`**

```vue
<script setup lang="ts">
import { ref } from 'vue'
import { ReplayPlayer } from '@/game/ReplayPlayer'
import type { ReplayResult } from '@/game/ReplayPlayer'
import type { EngineState } from '@engine'

defineProps<{ baseState: EngineState }>()
const fileInput = ref<HTMLInputElement | null>(null)
const result = ref<ReplayResult | null>(null)
const fileName = ref<string>('')
const isPlaying = ref(false)

async function handleFile(e: Event) {
  const input = e.target as HTMLInputElement
  const file = input.files?.[0]
  if (!file) return
  fileName.value = file.name
  const text = await file.text()
  const data = JSON.parse(text)
  // 期望文件格式：{ seed, engineVersion, initialState, inputs, expectedEvents }
  isPlaying.value = true
  const player = new ReplayPlayer({
    initialState: data.initialState,
    inputs: data.inputs ?? [],
    expectedEvents: data.expectedEvents ?? [],
  })
  result.value = player.run(data.maxTicks ?? 600_000)
  isPlaying.value = false
}
</script>

<template>
  <div class="replay-panel">
    <h3>Replay 工具（dev-only）</h3>
    <input ref="fileInput" type="file" accept=".json" @change="handleFile" />
    <div v-if="fileName">载入：{{ fileName }}</div>
    <div v-if="isPlaying">回放中…</div>
    <div v-else-if="result">
      <p v-if="result.divergedAt === null" style="color: green">
        ✅ 全部期望事件匹配，最终 tick={{ result.finalTick }}
      </p>
      <p v-else style="color: red">
        ❌ 第一处分叉于 tick={{ result.divergedAt }}（共记录 {{ result.events.length }} 条事件）
      </p>
    </div>
  </div>
</template>

<style scoped>
.replay-panel {
  position: fixed; bottom: 0; right: 0;
  background: #222; color: #eee; padding: 1em;
  font: 12px monospace; max-width: 400px;
  z-index: 9999;
}
</style>
```

- [ ] **Step 2：在 App.vue 仅在 `import.meta.env.DEV` 时挂载**

```vue
<ReplayPanel v-if="isDev" :base-state="engineState" />
```

```ts
import ReplayPanel from '@/components/ReplayPanel.vue'
const isDev = import.meta.env.DEV
```

- [ ] **Step 3：手动冒烟：导出一段事件后再载入面板验证 OK 报告**

操作：玩 30 秒 → 导出 events.json → 改造为 replay format（手动加 `initialState` 字段为开局快照）→ 拖入面板 → 看到绿色 ✅。

构造 replay JSON 的最简手动方式：在 App.vue 的 handleStationSelect 里同时存一个全局 `window.__INITIAL_STATE__`，dev-only。

```ts
if (import.meta.env.DEV) {
  ;(window as any).__INITIAL_STATE__ = JSON.parse(JSON.stringify(engineState.value))
}
```

- [ ] **Step 4：commit**

```bash
git add src/components/ReplayPanel.vue src/App.vue
git commit -m "feat: ReplayPanel.vue dev-only 面板用于载入回放并定位分叉"
```

---

## Phase J：模糊测试

### Task 32：模糊测试 100 局

**Files:**
- Create: `packages/engine/tests/fuzz.spec.ts`
- Modify: `package.json`

- [ ] **Step 1：写 fuzz 测试**

```ts
import { describe, it } from 'vitest'
import {
  buildAdjacency, createPRNG, randomChoice, randomFloat,
  setDevMode, step, type EngineInput, type EngineState, type RailMap,
} from '@engine'

function buildSyntheticMap(): RailMap {
  // 构造一个含 2 站台 + 双道岔 + 2 信号灯的环形小图
  return {
    nodes: {
      n_in:  { id: 'n_in',  x: 0,   y: 0, type: 'connector', signalState: 'green' },
      n_sw:  { id: 'n_sw',  x: 100, y: 0, type: 'switch', signalState: 'green', switchState: 0 },
      n_t1e: { id: 'n_t1e', x: 200, y: -10, type: 'connector' },
      n_t2e: { id: 'n_t2e', x: 200, y:  10, type: 'connector' },
      n_out: { id: 'n_out', x: 300, y: 0, type: 'endpoint' },
    },
    edges: {
      e_in:    { id: 'e_in',    fromNode: 'n_in',  toNode: 'n_sw',  length: 100, occupiedBy: null },
      t1:      { id: 't1',      fromNode: 'n_sw',  toNode: 'n_t1e', length: 100, occupiedBy: null, isPlatform: true },
      t2:      { id: 't2',      fromNode: 'n_sw',  toNode: 'n_t2e', length: 100, occupiedBy: null, isPlatform: true },
      t1_out:  { id: 't1_out',  fromNode: 'n_t1e', toNode: 'n_out', length: 100, occupiedBy: null },
      t2_out:  { id: 't2_out',  fromNode: 'n_t2e', toNode: 'n_out', length: 100, occupiedBy: null },
    },
    platforms: [],
  }
}

function initialState(seed: number): EngineState {
  return { tick: 0, trains: [], railMap: buildSyntheticMap(), rng: createPRNG(seed) }
}

function randomInput(state: EngineState, tick: number): EngineInput | null {
  const r = randomFloat(state.rng)
  if (r < 0.5) return null
  if (r < 0.8) {
    const switches = ['n_sw']
    return { tick, type: 'switch_toggle', payload: { nodeId: randomChoice(state.rng, switches) } }
  }
  const signals = ['n_in', 'n_sw']
  return { tick, type: 'signal_toggle', payload: { nodeId: randomChoice(state.rng, signals) } }
}

describe('Fuzz：100 局 × 100k tick 不应触发不变量违反', () => {
  it.skipIf(!process.env.RUN_FUZZ)('100 seeds passes invariants', () => {
    setDevMode(false) // 不抛错，只收 violations
    let totalViolations = 0
    for (let seed = 1; seed <= 100; seed++) {
      let s = initialState(seed)
      for (let i = 0; i < 100_000; i++) {
        const input = randomInput(s, s.tick)
        const r = step(s, input ? [input] : [])
        s = r.next
        for (const e of r.events) {
          if (e.kind === 'invariant_violation') totalViolations++
        }
      }
    }
    if (totalViolations > 0) {
      throw new Error(`Fuzz invariant violations: ${totalViolations}`)
    }
  }, /* timeout 10min */ 600_000)
})
```

- [ ] **Step 2：在 `package.json` 增加 fuzz 命令**

```json
"scripts": {
  "test:fuzz": "RUN_FUZZ=1 vitest run packages/engine/tests/fuzz.spec.ts --testTimeout=600000"
}
```

Windows 写法（环境变量）：实际跨平台用 cross-env：

```json
"test:fuzz": "cross-env RUN_FUZZ=1 vitest run packages/engine/tests/fuzz.spec.ts --testTimeout=600000"
```

并把 `cross-env` 加到 devDependencies。

- [ ] **Step 3：跑 fuzz**

Run: `npm run test:fuzz`
Expected: pass（如失败说明引擎有逻辑漏洞，先回头修引擎，再继续）

- [ ] **Step 4：commit**

```bash
git add packages/engine/tests/fuzz.spec.ts package.json package-lock.json
git commit -m "test(engine): 模糊测试 100 局 × 10 万 tick"
```

---

## Phase K：验收

### Task 33：历史 bug 复现 + 修复

**目标**：用现在的工具链定位**至少 1 例**已知偶发 bug（闯红灯 / 原地不动 / 出站不消失），写成回归测试，修引擎，提交。

**Files:**
- Create: `packages/engine/tests/regressions.spec.ts`
- Modify: `packages/engine/src/...`（视 bug 修哪个 phase）

- [ ] **Step 1：让用户/工程师在 dev 环境用 ReplayPanel 跑 30 分钟**

要求：开 setDevMode(true)，玩到不变量违反触发（throw）。记下当时的 seed + InputRecorder.snapshot()。

- [ ] **Step 2：把 seed + 输入流写成回归测试 `packages/engine/tests/regressions.spec.ts`**

```ts
import { describe, expect, it } from 'vitest'
import { step, createPRNG, type EngineInput, type EngineState } from '@engine'

const SEED = 0 /* 替换为实际触发的 seed */
const INPUTS: EngineInput[] = [
  /* 粘贴 InputRecorder.snapshot() 输出 */
]
const INITIAL_STATE: EngineState = {
  /* 粘贴启动时的 engineState 快照 */
} as any

describe('regression: <bug 描述>', () => {
  it('在已知 seed + 输入流下 100k tick 内不触发任何 invariant_violation', () => {
    const byTick = new Map<number, EngineInput[]>()
    for (const i of INPUTS) {
      const a = byTick.get(i.tick) ?? []
      a.push(i)
      byTick.set(i.tick, a)
    }
    let s: EngineState = JSON.parse(JSON.stringify(INITIAL_STATE))
    s.rng = createPRNG(SEED)
    for (let i = 0; i < 100_000; i++) {
      const r = step(s, byTick.get(s.tick) ?? [])
      s = r.next
      const violation = r.events.find((e) => e.kind === 'invariant_violation')
      if (violation) throw new Error(`Tick ${s.tick}: ${JSON.stringify(violation)}`)
    }
    expect(true).toBe(true)
  })
})
```

- [ ] **Step 3：跑测试确认失败（复现成功）**

Run: `npx vitest run packages/engine/tests/regressions.spec.ts`
Expected: 失败，错误信息含 violation 详情 + tick

- [ ] **Step 4：根据 violation 定位 phase，修复**

具体修哪个文件取决于触发的 rule：

- `R1_moving_zero_speed` 或 `R2_stuck_resumable` → 通常是 `phases/intent.ts` 中 tryResume 路径漏判
- `signal_pass` 异常 + `signal_state=red` → `phases/intent.ts` 红灯 branch
- `R3_handover_not_removed` → ExitProcessor.process 漏移除
- `R5_occupancy_mismatch` 或 `R7_missing_edge` → `phases/commit.ts` 转换逻辑

逐一修复直到回归测试通过。

- [ ] **Step 5：跑全部测试 + 类型检查 + fuzz**

Run: `npx vitest run`
Run: `npm run type-check`
Run: `npm run test:fuzz`
Expected: 全 pass

- [ ] **Step 6：commit**

```bash
git add packages/engine/tests/regressions.spec.ts packages/engine/src/
git commit -m "fix(engine): 修复历史偶发 bug — <简述> + 回归测试"
```

---

## 验收金标准（自动 + 人工）

按 spec § 10.1：

- [ ] `packages/engine/` 单测覆盖率 ≥ 80%（运行 `npx vitest run packages/engine --coverage`）
- [ ] PRNG 全面替换 `Math.random()`（grep 仅剩 PRNG seed 来源）
- [ ] 不变量断言 ≥ 8 条（框架 + R1–R7 = 8）+ 开发模式 throw 验证
- [ ] 事件流环形缓冲 + 导出按钮（手动测试导出 JSON 内容合理）
- [ ] Replay 面板（dev-only）能载入 JSON 并报告分叉
- [ ] App.vue 拆为 6 模块，每个 < 300 行；App.vue < 200 行
- [ ] 模糊测试 100 局 × 10 万 tick 通过
- [ ] **当前已知偶发 bug 至少 1 例被定位 + 修复**（Task 33）

---

## 后续阶段

阶段一完成后，由用户选择是否进入阶段二（后端骨架）。阶段二是另一份独立 plan，依赖本阶段产出物：
- `packages/engine/` 已可被 sidecar 复用
- 输入流格式已固定，可上传
- 不变量违反事件已结构化，可入库

